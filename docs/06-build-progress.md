# Zyndix Engine — Build Progress

**File:** `06-build-progress.md` · **Started:** 2026-07-08 · **Last reconciled:** 2026-09-26 (Session 19)
**Tracks:** `09-build-plan-v2.md`, which implements `08-complete-build-brief.md`.

**Status vocabulary** (`CLAUDE.md`, never collapsed):

| Status | Means |
|---|---|
| **not started** | no implementation exists |
| **implemented** | code exists and looks complete; its DoD has not been demonstrated |
| **tested locally** | its DoD script was actually run and passed, output in `07-build-log.md` |
| **verified with provider** | exercised against the real third-party service, separately reported |
| **active in production** | running against real prospects |

"The file exists" is never better than **implemented**. A mocked integration is never **verified with provider**. Where this file and any handoff disagree, **the repo wins**.

---

## 1. Prerequisites (outside the repo)

Filled from `.env.local` **key presence only** — no value was read, printed or recorded.

| Item | Status | Needed by | Notes |
|---|---|---|---|
| Supabase project `zyndix-engine` | ✅ ready | — | `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` present. Migrations verified applied by `scripts/ping.ts` 2026-09-21 |
| Anthropic API key | ✅ ready | — | Replaced 2026-08-23 after a `401`. Verified live. **Vercel env still holds the dead key** |
| Apollo API key + credits | 🟨 present, credits unverified | U15 | Plan/tier and remaining credits not confirmed. **Session 12: Organization Enrichment (`GET /organizations/enrich`) called live 4× (operator-approved; docs: 1 credit per organization; the responses carried no credit metadata)** |
| Apify account + token | 🟨 present, unverified | U15 | Balance not confirmed |
| MillionVerifier key + credits | ✅ ready (test volume) | U5 | `MILLIONVERIFIER_API_KEY` present. **495 free credits** (operator-reported 2026-09-25) — enough for testing, **top up before volume**. `NEVERBOUNCE_API_KEY` absent and superseded |
| Telegram bot + user IDs | ✅ ready | — | `TELEGRAM_BOT_TOKEN` + `TELEGRAM_ALLOWED_USER_IDS` present; approve path exercised 2026-09-21 |
| **Two sending domains** | ✅ **bought 2026-09-21** | U5 | `zyndixhq.com` and `getzyndix.com`. 301 → `zyndix.com` live on both. These are U5's allowed-sender list. `STEP-11-RUNBOOK.md` §A.1 |
| DNS — `zyndixhq.com` (Google Workspace) | ✅ verified | U5 | `dig` 2026-09-25: MX `smtp.google.com`, SPF `include:_spf.google.com ~all`, DKIM `google._domainkey` published, DMARC `p=none`. Operator reports SPF/DKIM/DMARC verified in Workspace. `07` Session 10 |
| DNS — `getzyndix.com` (Microsoft 365) | ✅ **DMARC present · mail-tester passed** | U5 | `dig` 2026-09-25: MX `getzyndix-com.mail.protection.outlook.com`, SPF `include:spf.protection.outlook.com -all`, DKIM `selector1`/`selector2._domainkey` CNAMEs resolve to M365 keys. **DMARC added by the operator via dmarcly; Session 11 `dig` shows exactly ONE `_dmarc` TXT** on 1.1.1.1, 8.8.8.8 and both authoritative NS (`dns1`/`dns2.registrar-servers.com`): `v=DMARC1; p=none; rua=mailto:…@ag.dmarcly.com; ruf=mailto:…@fo.dmarcly.com; sp=none;` — no CNAME at `_dmarc`. **Session 12: the operator re-ran mail-tester on the getzyndix mailboxes after DMARC — passed (operator-reported)** |
| Tracking CNAME (`track.`) | ✅ **not needed — closed** | — | Operator decision 2026-09-25 (§5): **no custom tracking domain**. Workspace "Disable Open Tracking" ON and "Always send first email as text-only" ON (operator-reported — the workspace API exposes neither), link tracking off. Enforced per campaign and read back live by `instantly-sender-campaigns.ts --verify` (Session 11). **Link tracking off confirmed in the Instantly UI by the operator (Session 12).** `dig`: no `track` CNAME on either domain, as intended |
| **Instantly Growth + 4 mailboxes** | ✅ **live, warming · daily_limit 1** | U4 | Bought 2026-09-24 (**Growth**, not Hypergrowth — §5). Live read 2026-09-25 (`live-instantly.ts --all`): workspace `Zyndix`, `plan_id pid_g_v2`, **4 accounts, all status active / warmup active / setup_pending false / warmup score 100**. Warmup started 2026-09-24 19:31–20:52 UTC (all four). **Session 11 read (`GET /api/v2/accounts`): `daily_limit=4` on all four** — the operator set it to **1** as a pre-first-send safety net, so the saved value differs (§6). `sending_gap=1`; `enable_slow_ramp` **true** on the zyndixhq accounts, **false** on the getzyndix ones. **4 campaigns** since Session 11 — one draft campaign per mailbox (sender pinning, below). **Session 12 re-read (`GET /api/v2/accounts`): `daily_limit 1`, `enable_slow_ramp false`, `sending_gap 1` on all four** — matches the operator's settings. Account names: Amir Ebadi (amir@getzyndix), **Amirhossein** Ebadi (amir@zyndixhq), Ingrida Silobrit (both ingrida@). The accounts API exposes no signature field. **Webhooks: 2 legacy hooks from 2025-10-15 (`email_sent`, `reply_received` → `hook.eu1.make.com`, no auth header) — open, §6** |
| **Sender-pinned campaigns** | ✅ **created (draft), verified read-back** | U5 | Session 11, operator-approved Instantly write: `instantly-sender-campaigns.ts --apply` created `zx-sender-<mailbox>` ×4, each `email_list` = exactly that mailbox, one step `{{zx_subject}}`/`{{zx_body}}`, `open_tracking false`, `text_only`/`first_email_text_only true`, `stop_on_reply`/`stop_for_company true`, `daily_limit 15`, status **draft**, **no leads**. `--verify` passes. `link_tracking` was sent `false` but Instantly does not echo it (§6). Ids recorded in `send_accounts.instantly_campaign_id` |
| `send_accounts` rows | ✅ **4 rows** | U5 | Session 11, operator-approved: `seed-send-accounts.ts --apply` — `amir@`/`ingrida@` on both domains, `provider instantly`, `health ok`, `ramp_started_on null` (the first real reservation starts the ramp). Plus settings `send_policy` v1 (§7) |
| Mailboxes | ✅ connected | U5 | `zyndixhq.com`: separate **Google Workspace** tenant, `amir@` + `ingrida@`, both licensed. `getzyndix.com`: separate **Microsoft 365** tenant, Business Basic, 2 licences (admin account unlicensed), `amir@` + `ingrida@`. **Same local parts on both domains** → one `send_account` per lead for the whole sequence (§6) |
| `INSTANTLY_API_KEY` | ✅ **verified** | U4 | Present in `.env.local`. Read scopes proven live 2026-09-25 (workspaces, accounts, campaigns, webhook event types). **`campaigns:create` proven live in Session 11** (4 draft campaigns created). **Session 12: webhook list / create / test / delete proven live on Growth** (§5). **Session 13: `emails:read` proven live** (`GET /api/v2/emails?email_type=received` answered, 0 items); webhook delete used again for the legacy pair. **Session 14 (U6 drill, operator-owned recipient only): `leads:create` (`POST /leads/add`), `campaigns:update` (activate + pause), `emails:create` (`POST /emails/reply`, no 402 on Growth), `GET /emails/{id}` all proven live on Growth.** Still **not exercised**: `leads:delete`, `block_list_entries:create` |
| Mailboxes pass mail-tester ≥9/10 | ✅ **passed, all four** | U5 | All four scored **9.6/10** (operator-reported). `zyndixhq.com` mailboxes: "properly authenticated". `getzyndix.com` mailboxes first showed "not fully authenticated" (DMARC absent); DMARC added (Session 11) and **the operator's re-test passed (Session 12, operator-reported)** |
| `INSTANTLY_WEBHOOK_SECRET` | ✅ ready (local) | U6 | Set by the operator in `.env.local` (Session 12), ≥32 chars; presence-checked only, never printed. Instantly has no HMAC signing, so this static token is sent by Instantly in `x-zyndix-webhook-token` and compared in constant time. **Must also go into the Vercel env at deploy (U23)** |
| Migrations `0009_send_prereqs.sql` + `0009b_exceptions.sql` applied | ✅ applied 2026-09-25 | U6 | Applied by the operator (Session 12) after a clean local Postgres 16 pre-flight. Columns `companies.hq_*`, `leads.timezone_source`/`timezone_derived_at`, `send_accounts.signature_text`, `webhook_events.processing_error`; table `exceptions` (RLS on). Exercised by `test-u6-webhooks.ts` (58/58) and `test-u5-send.ts` (67/67) |
| Mailbox signatures | ✅ **4 written** | U6 | Operator-approved (Session 12): `send_accounts.signature_text` = `Amir Ebadi / Zyndix, Vilnius / zyndix.com` on both amir@, `Ingrida Silobrit / …` on both ingrida@. Part of the approval hash |
| `TELEGRAM_WEBHOOK_SECRET` | ⬜ | U9 | Unset → `/api/webhooks/telegram` 500s on every request. Approvals run via `scripts/telegram-poll.ts` |
| `DASHBOARD_ALLOWED_EMAILS` | ✅ ready (local) | U1 | Present and non-empty in `.env.local`. Gates who may sign in and the role each is provisioned with (`email:role`; bare email = `viewer`). Vercel env **not verified** from inside the repo |
| `SUPABASE_ANON_KEY` | ✅ ready (local) | U1 | Present and non-empty in `.env.local`. Anon key, **not** service-role, and deliberately not `NEXT_PUBLIC_`. Vercel env **not verified** from inside the repo |
| Supabase Auth email provider + redirect URL | ✅ ready (localhost) | U1 | Magic-link round trip completed by the operator on localhost, 2026-09-24. Whether the production callback URL is allow-listed is **not verified** — check before deploy |
| Migration `0005_app_users_roles.sql` applied | ✅ applied | U1 | Proven by `test-u1-auth.ts`: `app_users` readable, check constraint rejects a bad role (`23514`), `source_cursors` trigger fires |
| Migrations `0006_jobs.sql` + `0006b_claim_jobs_rpc.sql` applied | ✅ applied 2026-09-24 | U2 | Applied by the operator in the SQL editor. `jobs.relrowsecurity = t`; `claim_jobs` executable by `postgres`, `service_role` and `supabase_admin` only (operator-reported). Exercised end to end by `test-u2-jobs.ts` |
| Migrations `0008_touch_approval_binding.sql` + `0008b_outbox.sql` applied | ✅ applied 2026-09-25 | U5 | Applied by the operator in the SQL editor (Session 11) after a clean pre-flight on local Postgres 16. Exercised end to end by `test-u5-send.ts` (64/64) and `test-draft.ts` (approval binding) |
| Migrations `0007_capacity_counters.sql` + `0007b_reserve_capacity.sql` applied | ✅ applied 2026-09-24 | U3 | Applied by the operator in the SQL editor. `capacity_ledger` and `capacity_reservations` both `relrowsecurity = true`; `reserve_capacity` and `settle_capacity` executable by `postgres` and `service_role` only (operator-reported). Exercised end to end by `test-u3-scheduler.ts` |
| Calendly webhook signing key | ⬜ | U8 | `CALENDLY_WEBHOOK_SIGNING_KEY` absent |
| Attio API key | ⬜ | U19 | Deliberately deferred 2026-07-13; nothing before U19 needs it |
| Heyreach account | ⬜ | U18 | External execution stays **off**; adapter completes without it |
| Embeddings provider | ⬜ open decision | U12 | Anthropic has no embeddings endpoint. FTS ships first and stays the labeled fallback |
| GitHub repo + Vercel linked | 🟨 | U23 | git repo live; Vercel link not verifiable from inside the repo |

---

## 2. Unit tracker — `09-build-plan-v2.md`

Units run in execution order. Phase 4 precedes Phase 2 by operator decision (§5, 2026-09-21).

### Phase 1 — Reconcile and repair *(4 sessions)*

| Unit | Name | Status | Evidence |
|---|---|---|---|
| — | Repair + adopt brief *(Session 3)* | ✅ **tested locally** | `test-draft.ts` rewritten against fixtures, 32/32 pass, non-fixture counts identical. `ping.ts` added, 3/3 pass. Docs realigned |
| U1 | Auth, roles, dashboard shell | ✅ **tested locally** | `scripts/test-u1-auth.ts --base-url http://localhost:3000` → **48/48**, non-fixture row counts unchanged (2026-09-24, `07` Session 6). Magic-link sign-in end to end as `admin`; all ten areas render (operator-verified) |
| U2 | Durable job system 🛒 | ✅ **tested locally** | `pnpm test:jobs` → **63/63** against Supabase, all four `09` §U2 DoD items, non-fixture row counts and `jobs` count unchanged (2026-09-24, `07` Session 8). No provider, so this is the highest status U2 can reach. The 🛒 purchase is operator-owned and was not a code blocker |

### Phase 4 (early) — Campaign execution *(18 sessions, incl. UD)*

| Unit | Name | Status | Evidence |
|---|---|---|---|
| U3 | Scheduler: ledger + send windows | ✅ **tested locally** | `pnpm test:scheduler` → **80/80** against Supabase: 30 concurrent reservations at quota 15 → exactly 15 `ok` / 15 `quota_exhausted`, `reserved = 15` (3 rounds); release decrements; windows table incl. both DoD cases, 3 DST dates, ±17 min jitter over 1000 draws; non-fixture counts unchanged (2026-09-24, `07` Session 9). No provider, so this is the highest status U3 can reach |
| U4 | Instantly adapter | ✅ **tested locally** · **read paths verified with provider** · write paths mocked only (except webhooks, below) · Session 12: webhook list/create/test/delete added, **77/77**, create/test/delete **verified live** | `pnpm test:instantly` → **63/63** (fetch mocked, synthetic fixtures): every `09` §U4 DoD case plus the full three-way taxonomy. `live-instantly.ts --all` against the real account 2026-09-25: workspace + **4 accounts**, 0 campaigns, warmup analytics, webhook event types — every response parsed by the fixture schemas, `secret material in output: none` (`07` Session 10). **Enroll / pause / activate / delete / block-list have never touched Instantly** — they reach *verified* only in U6 Part 2's live drill |
| U5 | Send stage, preflight, guards | ✅ **tested locally** · provider writes mocked · **Session 12: signature + sender bound into the approval** (`test:sending` 68/68, `test:send` 67/67) | `pnpm test:sending` → **51/51** (pure: 15-row preflight table with exact reasons, domain-guard sub-table, sender pinning, timezone rule, approval hash). `pnpm test:send` → **64/64** against Supabase with a mocked Instantly: happy path `approved → queued → sent` with exactly one adapter call and `accepted = 1`; `sender_mismatch` with no reservation; uncertain → zero re-sends + reconcile both ways; lease-expiry crash → zero extra calls; suppression between approval and send → `reserved` back to 0; `timezone_unknown` hold with no deferral; threaded follow-up via `emails/reply`; non-fixture counts unchanged (`07` Session 11). The live enroll/reply round trip is U6 Part 2. **Nothing is wired to cron** (U9) |
| U6 | Webhooks, reply freeze, suppression 🚩 | ⛔ **4 of 4 sessions done — blocked: follow-ups move to U6c** · Session 16 live re-test (alias `drill:s14b`): step 1 + `email_sent` **verified with provider** again; step 2 via `emails/reply` + `additional_recipients` reached the lead in step 1's thread **but To = `amir@zyndixhq.com, ebadiamirhoseineng+s14b@gmail.com`** (API and Gmail raw agree) → **STOP**; the engine recorded it `sent` (post-send check gap) · 🚩 **not reached** · Earlier: Part 1 **tested locally** · Part 2 live drill run (Session 14): step 1 enroll → `email_sent` webhook → reply webhook → freeze → poll dedupe **verified with provider**; threading headers **verified**; step-2 follow-up went to the **sender**, not the lead (§6 ⛔) — fix **tested locally** (mocks), re-test Monday | Sessions 12–13: see `07`. **Session 14 live drill** (recipient `ebadiamirhoseineng@gmail.com` only, sender amir@zyndixhq.com, drill lead `7fd018fa` tagged `drill:s14`): step 1 via the real send stage (queue + worker) → `leads/add` accepted, outbox `accepted`, touch `sent`, ledger accepted 1, lead `approved → queued → sent`; `email_sent` webhook ≈2.5 min later filled the thread anchor. Step 2 via `emails/reply` accepted and threaded (`In-Reply-To`/`References` = step 1's Message-ID, one Gmail conversation) **but addressed to amir@zyndixhq.com** (spec: default recipient = sender of the replied-to email). Operator reply to step 1 → `reply_received` webhook → lead `sent → replied`, inbound touch, 0 jobs to cancel, no Anthropic (no import on the path, no classifier event, dev log shows only webhook POSTs). `runReplyPoll` → 0 requests (lead already replied, by design); webhook `email_id` **=** `GET /emails` `id` for both sent and received; polled reply replayed through the processor → `duplicate_reply`, no second inbound touch. **Fix (mocks only):** `additional_recipients=[lead.email]` on every step ≥ 2 + fail-closed `reply_misaddressed` (accepted, never resent, touch `failed`, escalated exception, lead `manual_hold`, alert). `test:send` **78/78**, `test:instantly` **81/81**, `test:traversal` 62/62, `test:webhooks` 58/58. Cleanup: campaign 5392fcac paused, webhooks 0, tunnel + server stopped. 🚩 **not reached**: needs the Monday re-test (new lead `drill:s14b`) |
| U6b | Claim guard (interim slice) ⛔ gates prospect sends | ✅ **tested locally** (Session 15) · the v9 writer's claim ledger **verified with provider** (Anthropic, 1 live call on a synthetic fixture) · **not** a send | `pnpm test:claim-guard` → **87/87** against Supabase (mocked writer and Telegram, synthetic fixtures, row counts unchanged): all 10 `09` §U6b DoD cases plus the operator's Steffen case (quoted and unquoted "contact us to schedule a preview", not on the page → `unsupported_prospect_fact` "not in source page"; control passes), a malformed ledger → hold, a retry that fixes the draft, approval refused when evidence ages past 30 days, a ledger-less touch refused. Each held case: 0 touches, lead `manual_hold`, `claim_guard_hold` names the reason, exactly 2 writer calls, 1 alert. `pnpm test:claims` → **45/45** (pure checker; the 4 audit patterns). Approval binds the ledger: `test:sending` **69/69** (+1). Regressions green: `test:traversal` 62/62, `test:send` 78/78, `test:webhooks` 58/58, `test:jobs` 63/63, `test:scheduler` 80/80, `test:instantly` 81/81, `test:webhook-rules` 10/10, `test:apollo` 6/6, `test:source-filters` 9/9, `tsc`, `build`; lint unchanged from `main` (21 pre-existing in old scripts). Live `test-draft --limit 1` (writer v9): **39/39**, 6-claim ledger, guard passed on the first attempt, approval snapshot binds the ledger. Migration `0009c` applied by the operator. **Prospect sends stay blocked** by: the U6 re-test, warmup + inbox placement, stale evidence (re-crawl), and **UR** (operator decision, below) |
| U6c | Instantly-owned follow-up steps 🚩 | 🟨 **S20 done (Session 20) — next: S21** · S20 (enroll with every step's variables, preflight `sequence_incomplete` / `campaign_sequence_drift` / `provider_daily_limit`, engine step ≥ 2 send hard-disabled, adapter `updateCampaign`/`getLead`/`getEmail`/daily analytics, delay mapping M1, campaign `--update`, `step2_repeats_step1`, footer re-append) **tested locally**; writer v11 **applied** (operator OK); its one live draft (synthetic fixture) was **held by the claim guard** (`07` Session 20 addendum) · S19 **tested locally**, writer v10 **verified with provider** · S18 provider mechanics **verified with provider** · stops / tracking / recipient check not started (S21) | **S20:** `test:send` **97/97** (E1–E6 + pinning), `test:sequence` **113/113**, `test:campaign-sequence` **10/10** (M1, C1), `test:sending` **76/76**, `test:instantly` **92/92**, `test:sequence-rules` **38/38**, `test:claim-guard` 91/91, `test:traversal` 62/62, `test:webhooks` 58/58, `test:jobs` 63/63, `test:scheduler` 80/80, `test:claims` 45/45, `test-validation` **16/16**; tsc, build clean. Live read-only `--verify` / `--update` dry run: all 4 `zx-sender-*` single-step (DRIFT, expected until S22); `5392fcac` refused `campaign_has_leads`. **S19:** `pnpm test:sequence` **90/90**, `test:sequence-rules` **34/34**, `test:claim-guard` 91/91, `test:traversal` 62/62, `test:send` 78/78, `test:webhooks` 58/58, `test:jobs` 63/63, `test:scheduler` 80/80; live `test-draft --limit 1` 80/80 ($0.0167). **S18 spike** (`scripts/spike-u6c.ts`, script **tested locally**; no engine code, no DB writes, no Anthropic). Alias `ebadiamirhoseineng+s17@gmail.com` only, sender **amir@getzyndix.com**, campaign `d598def3` `zx-drill-s17-amir-getzyndix` (3 fixed steps, 5 min delays). **Verified with provider:** the API accepts an empty follow-up `subject` (sent as "Re: Zyndix spike S17"); step 2 To = the alias only, no Cc/Bcc, no own address (API + Gmail raw agree); same `thread_id`, `In-Reply-To`/`References` = step 1's Message-ID, one conversation; `text/plain; charset=utf-8` only (steps 1–2); webhook `email_sent` `step=2`, `email_id` = GET id; `DELETE /leads/{id}` 13 s after the step-2 webhook → GET 404, list 0 → **no step 3** (API, webhook, Gmail; watched to 20:30Z) and the campaign auto-completed; steps 1–2 still readable after the delete. **Findings for S19:** step 2 carries a quoted copy of step 1; API `step` is 0-indexed `"0_1_0"` vs webhook 1-indexed; API stores the body as HTML although the sent mail is text/plain. Earlier: planned Session 17 (plan in `09` §U6c; decisions in §5). Sessions: S19–S21 build · S22 engine drill + 4 prod PATCHes. ⛔ row in §6 stays open until S22 passes |
| U7 | Reply classifier + routing | ⬜ not started | `reply_classifier_prompt` v1 seeded; schema written; no stage consumes it |
| **UD** | **Apply design system** 🎨 | ⬜ not started | Added 2026-09-21. U1 ships the shell unstyled; the system is authored in Claude Design. Sits before U8, the first unit that renders real data |
| U8 | Calendly, meetings, booking stop | ⬜ not started | route is a `.gitkeep` |
| U9 | Orchestrator, crons, pause | ⬜ not started | both cron routes are `.gitkeep`; `lib/auth/cron.ts` exists |

### Phase 2 — Knowledge system *(11 sessions)*

| Unit | Name | Status | Evidence |
|---|---|---|---|
| U10 | Storage, upload, extraction | ⬜ not started | no Storage usage anywhere yet |
| U11 | PDF/DOCX, OCR, review, screening | ⬜ not started | |
| U12 | Search, retrieval, Ask the library | ⬜ not started | embeddings provider undecided |
| U13 | Structured catalog | ⬜ not started | `proof_points` v1 is the only asset store today |

### Phase 3 — Research and matching *(9 sessions)*

| Unit | Name | Status | Evidence |
|---|---|---|---|
| U14 | Campaigns, enrollments, prospect import | ⬜ not started | |
| U15 | Typed evidence model | ⬜ not started | builds on `enrich/core.ts` + `qualify/core.ts` |
| **UR** | **Research sources (Apify)** — with U15 · **before the first prospect send** | ⬜ not started · added 2026-09-25 (operator, Session 15) | Apify only, never the operator's own LinkedIn. Person + company LinkedIn posts, LinkedIn profile, job posts, Google reviews, news / company blog, each as typed evidence (URL + date + excerpt) the claim guard can verify. Scope in `09` §UR; sessions to be estimated at its planning session |
| U16 | Matching and recommendations ⭐ | ⬜ not started | ⭐ the brief's central acceptance criterion |
| U17 | Draft rewired to matching, approvals UI | ⬜ not started | |

### Phase 5 — Integrations and commercial workflow *(12 sessions)*

| Unit | Name | Status | Evidence |
|---|---|---|---|
| U18 | Heyreach + manual LinkedIn (OFF) | ⬜ not started | |
| U19 | Attio two-way sync | ⬜ not started | supersedes the deferred step 8 |
| U20 | Inbox, briefs, pipeline, opportunities | ⬜ not started | |
| U21 | Costs, reports, digest, learning | ⬜ not started | |

### Phase 6 — Verification and handoff *(5 sessions)*

| Unit | Name | Status | Evidence |
|---|---|---|---|
| U22 | Dashboard pass, e2e, chaos, red-team | ⬜ not started | |
| U23 | Fresh-project migrations, handoff | ⬜ not started | |

**Live pipeline as of 2026-09-21:** 34 companies · 34 leads — `parked` 19, `qualifying` 10, `pending_approval` 3, `approved` 1, `enriching` 1 · 219 lead_events · 4 touches · 0 leads have reached `sent`. **Since 2026-09-25 (Session 11): 4 `send_accounts`**. All 34 companies have `country = 'US'` and null timezones, and all 34 leads have null timezones, so under the U5 timezone rule every current lead would be held `timezone_unknown` until a timezone is set.

---

## 3. Carried-over status of steps 1–10 (`05-build-plan.md`)

The old 15-step plan is superseded by the unit tracker above, but the code those steps produced is real and most units reuse it. Restated honestly in the four-word vocabulary:

| Old step | What exists | Status | Why not higher |
|---|---|---|---|
| 1 Scaffold & foundations | Next 16.2.10, TS strict, `lib/db`, `auth/cron.ts`, `scripts/ping.ts` | **tested locally** | `ping.ts` written and passing 2026-09-21. It is read-only: migration `0001` drops `_ping`, so the original write-a-row DoD is unrunnable by design |
| 2 Migrations | `0000`–`0004`, 17 tables, RLS on 16, `transition_lead` RPC | **implemented** | Applied to the live DB and proven reachable by `ping.ts`, but no per-table insert/read DoD script exists. `source_cursors` lacks RLS — fixed in U1 |
| 3 Settings system | `settings/core.ts` (210), seed content (314) | **tested locally** | `test-settings.ts` 8/8 |
| 4 State machine | `state/core.ts` (122) + `0002` RPC | **tested locally** | `test-state.ts` 11/11 |
| 5 Apollo + source | `apollo.ts` (389), `source/` (559) | **implemented** | `test-source.ts` withheld on Apollo quota. `test:source-filters` 9/9 covers name-guard units only |
| 6 Apify + enrich | `apify.ts` (295), `enrich/core.ts` (540) | **implemented** | `test-enrich.ts` withheld on real actor spend. Templates at v3 (`playwright:adaptive`), **never re-tested against the site that failed** |
| 7 Anthropic + qualify | `anthropic.ts` (167), `qualify/core.ts` (661) | **verified with provider** | `test-qualify.ts --limit 1` 3/3, 2,520 tokens, $0.0093, live API. Lead `200c7e06` correctly parked with a `disqualify_reason` |
| 8 Attio sync | placeholders only | **not started** | Deliberately deferred 2026-07-13. Returns as U19 |
| 9 Verification stage | `millionverifier.ts` (149), `verify/core.ts` (418) | **implemented** | `test-verify.ts` withheld — spends Apollo **reveal** credits plus MillionVerifier credits |
| 10 Writer + Telegram | `draft/core.ts` (542) + `guard.ts` (325), `telegram/handler.ts` (643) | **tested locally — approve path** | `test-draft.ts` 32/32 on 2026-09-21: draft → `pending_approval` → Telegram → ✅ → `approved`. **The ✏️ edit and ❌ kill paths remain undemonstrated** — each needs its own fixture |
| 11 Instantly send | — | **not started** | Becomes U3–U6 |
| 12 Webhooks | `.gitkeep` | **not started** | Becomes U6, U8 |
| 13 Reply classifier | `.gitkeep` | **not started** | Becomes U7 |
| 14 Orchestrator | `.gitkeep` | **not started** | Becomes U9 |
| 15 Dashboard | `.gitkeep` | **not started** | Becomes U1 (shell) + U22 (full pass) |

**Nothing is "active in production".** Zero leads have reached `sent`.

---

## 4. Phase 0 tracker (manual warm outreach — parallel, not optional)

| Item | Status | Notes |
|---|---|---|
| 20 LT target companies listed | ⬜ | |
| Messages 1–5 sent | ⬜ | |
| Messages 6–10 sent | ⬜ | |
| Messages 11–20 sent | ⬜ | |
| Replies / meetings logged | ⬜ | replies: _ · meetings: _ |
| Best 5 promoted into writer prompt | ⬜ | writer is already at v7 from live iteration, so this becomes v8 |
| 4 testimonial requests sent | ⬜ | Fonderis, ScholarCert, PulseConf, Loveko — drafts exist, pending since late July |

This depends on nothing in the build and is the cheapest source of real hypothesis/evidence pairs. It is still the highest-value item that is not code.

---

## 5. Decisions log

| Date | Decision | Why |
|---|---|---|
| 2026-07-08 | One orchestrator + stage workers, no agent swarm | determinism, debuggability, cost |
| 2026-07-08 | Supabase = brain, Attio = thin human window | full data ownership; Attio's 3-object cap irrelevant |
| 2026-07-08 | Cron-pull over event/queue architecture | replayable, zero queue infra at v1 volume |
| 2026-07-08 | Telegram over Slack for approvals | mobile speed, free bot API, 2-person team |
| 2026-07-08 | Buy pre-warmed domains; still ramp from 15/day | warmup ≠ immunity |
| 2026-07-08 | Skip Clay, Reply.io, Wappalyzer at v1 | duplication vs own Claude layer; HTML tool-detect covers most of it free |
| 2026-07-08 | Verification mandatory before send | bounce >3% burns inboxes |
| 2026-07-08 | Evidence-required rule enforced at DB level | the anti-generic guarantee |
| 2026-07-08 | Cold sends never from zyndix.com — code guard | domain reputation is unrecoverable |
| 2026-07-08 | Ads conversion module: capture now, upload later | standing instruction: don't jump ahead on ads |
| 2026-07-13 | Step 8 (Attio) deferred | read-only human window; nothing downstream depends on it |
| 2026-07-13 | **MillionVerifier chosen over NeverBounce** | implemented as `integrations/millionverifier.ts` |
| 2026-07-13 | Apify crawler → `playwright:adaptive` | cheerio returned zero pages on JS-rendered sites |
| 2026-08-23 | LinkedIn senders as a `settings` key, not code | sender choice is a per-campaign judgement; hardcoding means a deploy to change who sends |
| 2026-08-23 | Instantly Hypergrowth over Growth | time, not budget, is the binding constraint |
| 2026-08-23 | API keys stay in env vars; dashboard shows status only | a dashboard auth bug exposing stored service-role keys is unrecoverable |
| 2026-08-23 | Telegram long-poll stands in for the webhook | `TELEGRAM_WEBHOOK_SECRET` unset; unblocks approvals without a public URL |
| 2026-08-23 | `✅` requires a passing DoD run, not file existence | the tracker drifted for six weeks because "built" and "verified" shared one symbol |
| **2026-09-21** | **`08-complete-build-brief.md` adopted as scope authority** | It supersedes docs 01–05 wherever they conflict. Docs 01–05 keep a banner saying so rather than being deleted — they remain the record of how existing code was built |
| **2026-09-21** | **Build runs in Claude Code (desktop app), not Cursor** | Decided 2026-08-23, now in force. The remaining DoDs are script-based; Claude Code closes the run-read-fix loop without a human relay |
| **2026-09-21** | **Phase 4 executes before Phase 2** | Every first touch is operator-approved in Telegram and the proof line is operator-written, so the approval gate covers the generic-copy risk. The binding constraint on revenue is that `stages/send.ts` does not exist, not that a library does not exist. First send runs on the existing draft stage with a minimal single-campaign config; U14/U17 migrate it onto campaigns and matching |
| ~~2026-09-21~~ | ~~Sending domains bought now; Instantly bought at U3~~ | **Superseded the same day — see the next row.** The premise (that the domains' DNS could be completed immediately) was wrong |
| **2026-09-21** | **Domains bought; DNS and the Instantly purchase both move to U2** | Domains were bought and 301'd to `zyndix.com`; the rest of their DNS was deferred. That deferral was not optional: **DKIM is generated in Google Workspace Admin, and Workspace is part of the Instantly/mailbox purchase**, so clock A could never have delivered "full DNS". Moving the purchase from U3 (≈ day 11) to U2 (≈ day 7) buys four days, which is what funds the DNS work on purchase day. Warmup goes from ~21 to **~24 days** before FIRST SEND READY at U6 (≈ day 33) |
| **2026-09-21** | **The dashboard ships unstyled; the design system lands at UD** | The design system is being authored in Claude Design, outside the repo. Styling U1's shell in code before it exists would be thrown away. U1 therefore ships plain semantic HTML — no colour, no spacing scale, no components — and new unit **UD** applies the system just before U8, the first unit that renders real data. Styling earlier means styling empty pages; later means restyling |
| **2026-09-21** | **Dashboard roles bootstrap from `DASHBOARD_ALLOWED_EMAILS`, then the DB is authority** | The env gates who may sign in and what role they are *provisioned* with (`email:role`, bare email = `viewer`). After first login `app_users.role` wins and is never overwritten from the env, so changing someone's role is a DB update, not a redeploy. Rejected "first user wins admin" — privilege should not depend on who clicks first |
| **2026-09-21** | **`app_users.role` carries a `check` constraint — the schema's first** | The repo enforces state legality in application code and has no other check constraints. A role is a privilege boundary, so an unrecognised value must not be insertable at all. Recorded as a deliberate exception, not drift |
| **2026-09-21** | **MillionVerifier replaces NeverBounce — docs corrected, not just contradicted** | The 2026-07-13 decision was real but docs 01–05 kept saying "NeverBounce" for two months. They now carry an explicit drift note |
| **2026-09-21** | **Tests use isolated synthetic fixtures, never real prospects** | `test-draft.ts` reset 6 real leads and deleted their touches. Fixtures plus a before/after non-fixture row-count assertion make that class of bug detectable rather than silent |
| **2026-09-24** | **`jobs.attempts` increments at claim, not at failure** | A handler that crashes its worker never reaches `fail()`. Counting at claim means a crash still spends an attempt, and `claim_jobs()` dead-letters an expired lease on its final attempt (`lease_expired_after_final_attempt`) instead of re-claiming a poison job forever |
| **2026-09-24** | **Every job write after claim is lease-fenced** (`id + state='leased' + lease_owner + attempts`) | A worker that overran its lease and was superseded must not overwrite the new holder. Its late `complete()`/`fail()` returns `lease_lost` and changes nothing |
| **2026-09-24** | **The worker claims one job at a time** | A budget stop can then never strand a job that was leased but not started. One RPC per job is negligible at this volume; revisit only if claim overhead shows up in a cron's budget |
| **2026-09-24** | **`jobs.state` carries a check constraint** — the schema's second, after `app_users.role` | An unrecognised state is invisible to every claimer: the job would be silently lost. Same reasoning as the role constraint |
| **2026-09-24** | **New `security definer` functions pin `search_path` and revoke PUBLIC execute** | `claim_jobs` does both. `0002`'s `transition_lead` does neither — still in the `09` §5 backlog, not fixed here |
| **2026-09-24** | **Send-window tier rule: priority lookahead** (operator decision, Session 9) | `09` §U3's two DoD window cases conflict under a plain earliest-window rule: from Sun 23:00 and from Fri 16:30 the next two windows are the same (Mon 13:30 secondary, Tue 08:30 priority), yet one case expects Tuesday and the other Monday. Rule adopted: take the next priority window if it opens within `priority_lookahead_hours` (default **48**), else the earliest window of either tier. The field is optional on `send_windows`, so v1 still parses and no settings row was written; changing it is a new settings version |
| **2026-09-24** | **Capacity reservations are rows (`capacity_reservations`), not just counters** | `09` §U3 names only the four counters. But U2 jobs can be re-claimed and re-run, and a counters-only ledger would double-decrement on a retried release. Every counter change is a state transition on a reservation row inside `reserve_capacity()`/`settle_capacity()`, so it applies at most once; repeating a reached state returns `already`. Idempotency keys make a re-run reservation return the first one |
| **2026-09-24** | **An uncertain send keeps its capacity held until reconciled** | A timeout after possible acceptance may have sent the message. It keeps counting in `reserved` until reconcile says `sent` (→ `used`, `reconciled`) or `not_sent` (→ freed, `reconciled`). Full table in `0007`'s header |
| **2026-09-24** | **A day's quota only goes down; it is counted per sender per UTC day** | The first reservation sets the day's quota from the ramp; a later lower quota lowers it, a higher one does not raise it. The ledger date is the UTC date of the send instant (CLAUDE.md: timestamps in UTC) |
| **2026-09-24** | **The ramp anchors on new `send_accounts.ramp_started_on`; `daily_quota` is not used** | The ramp (15→30, +5 every 4 days) needs a start date and `send_accounts` had none. Null = not started → `rampQuota()` returns null, never 0. `daily_quota` (default 0 in `0001`) is ambiguous and superseded by the ramp |
| **2026-09-24** | **Work directly on `main`: no feature branches, no PRs** | Operator decision after U2. Units U1 and U2 each went through a branch and a PR (#1, #2), and for a single operator that is a round trip with no reviewer on the other end. The safety net is unchanged: every unit still ends with a passing DoD, real output in `07`, one commit and an operator-run `git push origin main`. Supersedes the per-unit `feat/*` branches used for U1 and U2. `CLAUDE.md` §Session discipline updated |
| **2026-09-25** | **Instantly Growth, not Hypergrowth** — supersedes the 2026-08-23 row | Operator purchase, 2026-09-24. Live `plan_id pid_g_v2`. The API and the webhook event-types endpoint are reachable on Growth (proven live). Whether Growth can **create** webhooks is unproven until U6 — creating one is a write |
| **2026-09-25** | **Two sending tenants: Google Workspace (`zyndixhq.com`) and Microsoft 365 (`getzyndix.com`)** | Provider diversity: one tenant's reputation or outage does not take down all sending. Mailboxes are `amir@` + `ingrida@` on both — this supersedes the runbook's `a.ebadi@` / `i.silobrit@` plan |
| **2026-09-25** | **A lead keeps one `send_account` for its whole sequence — never rotate mid-sequence** | The same display names (`amir@`, `ingrida@`) exist on both domains, so a follow-up from the other domain looks like a different sender with the same name. Instantly has **no per-lead sending-account field**, so U5 pins it structurally: one Instantly campaign per `send_account` plus an engine-side lead→`send_account` binding and a `sender_mismatch` preflight refusal (`09` §U5) |
| **2026-09-25** | **Instantly mutations: any 5xx is an uncertain outcome, not retryable** | `09` §U4 said "429/5xx → retryable". A 5xx after a POST may have been committed server-side; treating it as retryable permits exactly the resend §10 forbids. Reads keep 5xx → retryable (once). 429 stays retryable everywhere — it is rejected before processing. Connect-phase failures (`ECONNREFUSED`, `ENOTFOUND`, `EAI_AGAIN`, `UND_ERR_CONNECT_TIMEOUT`) are retryable even for mutations — nothing reached the server |
| **2026-09-25** | **Enrollment uses `POST /api/v2/leads/add` with one lead, `skip_if_in_workspace` on by default** | Instantly has **no idempotency-key header**. The bulk endpoint is the only create that reports skips explicitly (`leads_uploaded`, `skipped_count`, `in_blocklist`…), and the skip flags make a replayed enrollment a no-op instead of a duplicate. Inconsistent counts on a 200 are treated as uncertain |
| **2026-09-25** | **No custom tracking domain** (operator decision, Session 11) — closes the tracking-CNAME item as not needed | Opens are diagnostic and unreliable (brief §8) and are not used as a signal here, so a `track.` CNAME buys nothing. Workspace: "Disable Open Tracking" ON, "Always send first email as text-only" ON, link tracking off. Each sender campaign also sets `open_tracking:false`, `link_tracking:false` and `text_only`/`first_email_text_only:true`, read back by `--verify`. Supersedes runbook §B.4's "open tracking ON through a custom domain" |
| **2026-09-25** | **Timing is engine-owned; follow-ups are threaded replies** (operator decision, Session 11) | Brief §10 asks for one owner of timing per channel. The engine owns it: step 1 is enrolled into the bound sender's **single-step** campaign only inside the recipient's window; every step ≥ 2 is `POST /api/v2/emails/reply` (`reply_to_uuid` = step 1's Instantly email id, `eaccount` = the bound mailbox), so it lands in step 1's thread as `Re: <subject>`. The operator's condition was that follow-ups arrive in the same thread. The spec documents the reply endpoint and a shared `thread_id`; RFC `In-Reply-To`/`References` headers are **not** documented and are verified in the U6 drill (§6) |
| **2026-09-25** | **The cold-sender allow-list is a code constant** (`sending/guard.ts`: `zyndixhq.com`, `getzyndix.com`) | A DB or settings row could be edited to widen it. The `zyndix.com` block is evaluated before the list, has no override parameter, and would still refuse even if `zyndix.com` were added to the list. Adding a sending domain is therefore a reviewed code change |
| **2026-09-25** | **Send thresholds live in the versioned `send_policy` settings key**; v1 is conservative | `verification_max_age_days 90`, `allow_catch_all false`, `min_warmup_score 80`, `duplicate_company_window_days 30`. **Catch-all is not sent to in v1** — MillionVerifier's mapping also files `unknown` under `catch_all`, and a bounce rate above 3% burns an inbox. Allowing it is a v2 with a change note. A missing key holds every send; it is never defaulted in code |
| **2026-09-25** | **`timezone_unknown` is a hold, not a deferral** (operator change to the U5 plan) | With no timezone there is no next window, so deferring would loop forever. Resolution order: `leads.timezone` → `companies.timezone` → the HQ country **only if it has exactly one IANA zone** (IANA `zone.tab`, tzdata 2026c, applied strictly: DE, ES, PT and CY are excluded along with US/CA/AU/BR/RU/MX). Otherwise: refuse `timezone_unknown`, write `send_refused`, release any reservation, no re-enqueue, Telegram alert to the operator. The lead stays `approved` |
| **2026-09-25** | **An uncertain send is reconciled, never resent; proven absence goes to `manual_hold`** | Reconcile asks Instantly (`leads/list` for step 1, `GET /emails` for replies). Found → sent. Proven absent → `reconciled_not_sent`, the lead goes `queued → manual_hold` (the only legal exit) and the operator is alerted. An automatic resend after proven absence is a policy call this engine does not make on its own. `skip already_enrolled` is treated as uncertain too, since it may be our own earlier dispatch |
| **2026-09-25** | **Suppression scope: a row with an email is person-level only** | The verify stage writes both `email` and `domain` for an invalid address; treating that `domain` as company-wide would suppress every colleague. Only rows with a null email and a domain are company-wide. Matching is case- and whitespace-insensitive |
| **2026-09-25** | **Preflight runs twice: before reserving, and again on a fresh load after reserving** | The second pass is the "immediately before execution" check (brief §10). It is what turns a suppression, reply or booking that lands between reservation and dispatch into a refusal with the capacity released (DoD: `reserved` back to 0) |
| **2026-09-25** | **The adapter never retries a mutation; `accountHealth` takes its score threshold from the caller** | Retrying writes belongs to the U2 job system, where attempts are counted and fenced. The warmup-score threshold is policy, so it comes from settings in U5, not a constant in the adapter; without one the score is reported, not judged, and a missing score is `unknown`, never 0 |
| **2026-09-25** | **US timezone from the HQ state, never a guess** (operator item, Session 12) | Static state→IANA table (`sending/us-timezones.ts`). Single-zone states map directly; AZ is `America/Phoenix` except listed Navajo Nation communities. The 14 split states resolve only from an explicitly listed city on either side of the line. Missing state, unknown state, or unlisted city → no timezone, and the `timezone_unknown` hold stays. Stored as `leads.timezone` with `timezone_source` `hq_state`/`hq_state_city`. HQ location came from Apollo Organization Enrichment, for the 4 send candidates only (4 credits, operator-approved) |
| **2026-09-25** | **The sending mailbox's plain-text signature is part of the approval** (operator item, Session 12) | Instantly API sends carry no mailbox signature. The approval snapshot now includes `send_account_id` and `signature`; the send uses `composeOutboundBody(body, signature)`, the same function the hash is built from. A signature edited after approval, or a different mailbox, makes preflight refuse `stale_approval`; no signature → `sender_signature_missing`. Touches approved under the U5 snapshot are stale by construction |
| **2026-09-25** | **The sender is fixed when the operator approves, not at send time** (operator decision, Session 12) | Required for the signature to be in the hash. A lead's binding wins (follow-ups never rotate); otherwise step 1 takes the least-loaded eligible mailbox (health ok, campaign, signature, domain guard) within `send_policy.assignable_senders`. The Telegram APPROVED update shows the From line and the exact final text. Daily capacity stays a send-time question |
| **2026-09-25** | **Signature goes right before the compliance footer; the footer drops "— Amir"** (operator decision, Session 12) | The v2 footer already signed as Amir, so appending would have named him twice and signed ingrida@ mail as Amir. `compliance_footer` v3 = address + STOP line only; the signature is inserted before the final paragraph when it carries the opt-out line. `writer_prompt_email` v8 forbids a sign-off, and approval refuses any body that still signs itself (`— Name`, `Best,\nName`) |
| **2026-09-25** | **Only amir@ mailboxes are assigned for now** (operator decision, Session 12) | The writer persona is Amir. `send_policy` v2 `assignable_senders = [amir@zyndixhq.com, amir@getzyndix.com]`; the ingrida@ mailboxes keep warming. A per-sender persona is backlogged. At `daily_limit 1` that is 2 first touches a day |
| **2026-09-25** | **Instantly webhooks: static token header, persist first, hash dedupe** | The official spec has no HMAC or signing secret; the only auth is the webhook's `headers` map. We set `x-zyndix-webhook-token` = `INSTANTLY_WEBHOOK_SECRET` (≥32 chars) and compare in constant time; unset → 500, wrong → 401, zero rows either way. The payload carries no event id, so `external_id` = sha256 over {event_type, workspace, campaign_id, lead_email, email_id, step, timestamp}. A redelivery of a processed event is a no-op; an unprocessed one is replayed (every step is idempotent). Processing errors return 500 so Instantly redelivers. Subscribed with `all_events`, since the spec and guide event lists disagree |
| **2026-09-25** | **An auto-reply is not a reply** (operator decision, Session 12) | `auto_reply_received`, `lead_out_of_office`, an `is_auto_reply` flag, or an unmistakable auto-reply subject (`Automatic reply:`, `Out of Office`, …) → a `lead_events` `auto_reply` with any stated `return_date`. No state change, no job cancel, no touch kill, no inbound touch (that would trip `reply_freeze`). Anything unclear counts as a human reply and freezes — the safe error |
| **2026-09-25** | **A human reply freezes everything, on every channel, before any model call** | Inbound touch first (preflight's `reply_freeze` sees it immediately), then queued jobs referencing the lead or its touches are cancelled, unsent outbound touches killed, and the lead goes `sent → replied`. A reply on a still-`queued` lead is proof of sending: `queued → sent → replied`. The uncertain outbox row is still settled by the reconcile job from the provider, so ledger accounting has one owner. The webhook deps type contains no model client at all |
| **2026-09-25** | **Unsubscribe and bounce suppression are person-level; the verify and source stages now use the same scope** | Opt-out → a suppression row with the email, `do_not_contact`, lead `suppressed`, every channel frozen, then the Instantly block list; a failed block-list write is an escalated `stop_failed` exception (the engine is already stopped). Bounce → `email_status invalid`, suppression, `sent → bounced`, and `bounce_rate_7d` recomputed; above `capacity_defaults.auto_pause` the account goes `health = paused` (preflight refuses it) and its campaign is paused. `isSuppressed` in verify and source now delegates to `checkSuppression`, and lookups fail closed |
| **2026-09-25** | **`stop_processing_stale` fails closed** (Session 13) | A sender with an outbound touch accepted 25h–7d ago and **zero** Instantly webhook events (matched by `campaign_id` or `email_account`) since the oldest such send is treated as having no working stop path: `health = paused` + reason, its Instantly campaign paused (failure → escalated `stop_failed`), an escalated `stop_processing_stale` exception, one alert. Rows the reply poll writes carry `payload.source = reconcile_poll` and never count as activity, so polling cannot mask a dead webhook. The false-positive (a quiet sender paused) is the safe error |
| **2026-09-25** | **Missed replies are polled through the webhook processor; no cursor table** (Session 13) | Per sender, `GET /api/v2/emails?email_type=received&eaccount=…&sort_order=asc` from the oldest send to a lead still awaiting a reply (`queued/sent/no_reply/sequence_done`), floored at 30 days; no awaiting lead → no request. Each unseen reply becomes a deterministic `reply_received` payload fed to `processInstantlyEvent`, so the freeze is identical. Cap: 3 pages per sender, 10 requests per run; 429 or cap → `truncated`, next tick resumes; two truncated runs in a row escalate one `reply_poll_truncated` exception, a complete run resolves it. Mail from unknown leads is ignored here (the webhook path owns those exceptions) |
| **2026-09-25** | **A reply is deduped by Instantly email id across delivery paths** (Session 13) | `handleReply` returns `duplicate_reply` (no second `lead_events` row) when an inbound touch with that `provider_message_id` exists **and** the lead is already frozen or terminal; a half-finished earlier attempt still replays every idempotent step. Auto-replies dedupe on the `auto_reply` event's `email_id` |
| **2026-09-25** | **A paused sender makes zero provider calls at send** (Session 13) | `health ≠ ok` skips the Instantly account/warmup reads and the step-1 anchor lookup; preflight refuses `sender_unhealthy` on `health` alone (U6 DoD "zero provider calls") |
| **2026-09-25** | **Redraft is an explicit state edge** (operator decision, Session 13) | `pending_approval → drafting` and `approved → drafting`, event `redraft_requested`; the old touch is set `killed` (never deleted) first. Nothing after dispatch (`queued`, `sent`, …) can go back to drafting. The draft stage now also rejects a self-signed body with one revision retry, so v8's no-sign-off rule is enforced, not just prompted |
| **2026-09-25** | **The 8 stop rules proven by the traversal** (operator choice, Session 13) | Pipeline-wide: (1) no evidence → null hypothesis → parked at qualify, (2) invalid email → parked + suppression, (3) email suppressed after approval, (4) domain suppressed after approval, (5) reply before the send, (6) unsubscribe (email + LinkedIn jobs cancelled), (7) meeting booked, (8) sender paused by `stop_processing_stale`. Stale approval, the `zyndix.com` guard, quota and window stay covered by `test:send`/`test:sending` |
| **2026-09-25** | **No prospect send until U6b (claim guard) passes** (operator decision, Session 13) | U6 proves the engine can *stop*; it does not prove a draft is *true*. The read-only audit of the 4 v8 drafts found invented timings, an invented place, an unbacked "I've mapped out" asset claim, a claim contradicted by the site (REBG chat icon), and dates already in the past, all on 74-day-old evidence. U6b is a new 1-session unit after U6 (`09` §U6b): the writer tags each claim with evidence ids and a deterministic guard refuses (a) weekday/time-of-day assertions, (b) "I've mapped out / prepared …" with no real asset, (c) offer text other than the approved CTA until the knowledge library exists, (d) evidence older than 30 days, (e) contradicted evidence. Approval re-runs it. The full version is recorded under U15 (typed evidence ids) and U17 (+1 session). The U6 drill (operator-owned recipient) is unaffected |
| **2026-09-25** | **Stale drafts are killed and their leads parked, not held** (operator decision, Session 13) | The 4 v8 touches → `killed` (rows kept). Leads `pending_approval → parked`, `park_reason stale_evidence_2026-07-13`, so they can be re-crawled later (`parked → enriching` is a legal edge). No spend |
| **2026-09-25** | **Follow-ups name the lead in `additional_recipients`, and a reply not addressed to the lead fails closed** (operator decision, Session 14) | `POST /api/v2/emails/reply` has no `to` field; its default recipient is the sender of the replied-to email, i.e. our own mailbox for a follow-up to our step 1 (proven live in the drill). The send stage now passes `[lead.email]`; if the accepted email's `to_address_email_list` lacks the lead, the outbox settles `accepted` (never resent, capacity spent), the touch is `failed`, an escalated `reply_misaddressed` exception + alert is raised and the lead goes to `manual_hold`. Whether our own mailbox stays visible in To is unknown until the Monday re-test; **if it does, stop** — operator's fallback preference is Instantly-owned sequence steps with all texts approved up front |
| **2026-09-25** | **No prospect send until warmup completes AND an inbox-placement test passes** (operator decision, Session 14) | The drill's step 1 (amir@zyndixhq.com → operator Gmail) landed in **Spam** on warmup day 2 (reason not captured; marked "Not spam"). Adds to the U6b gate; U6b alone does not unlock prospect sends |
| **2026-09-25** | **Drill leads are real rows, tagged, never deleted** (Session 14) | Drill lead `7fd018fa` (company `ZX DRILL S14`, `segment='drill'`, domain `drill-s14.zyndix-drill.invalid`, lead_event `drill_tag {tag:"drill:s14"}`) is left in its final state `replied`. U7's classifier and any lead listing must exclude `segment='drill'` (backlog). Monday's re-test uses a NEW drill lead tagged `drill:s14b` |
| **2026-09-25** | **The only approved offer line is "Happy to write up what I'd change, if that's useful."** (operator decision, Session 15) | `cta_variants` v2's active variant was an instruction with example lines, and two of them ("Want me to send them over?", "Want the three?") imply an asset that does not exist. A deterministic "offer only from approved CTA text" check needs a literal allowlist: `cta_variants` v3 carries `approved_lines`, and the claim guard refuses any other offer sentence and any offer language outside it (`unapproved_offer`). Lifted only by U13/U17 approved facts |
| **2026-09-25** | **Prospect facts must also be on the raw crawled page** (operator addition, Session 15) | The qualifier paraphrases (the audit found a misquoted E3 and a $1.2M not on the page). When raw `apify_site` text exists, a `prospect_fact` citing a `website` item fails with `unsupported_prospect_fact` "not in source page" if any fact token, any quoted fragment in the span, or any quoted fragment of the cited evidence that the span reuses is not in that page text. Claims citing only `apollo` items skip the page check (firmographics are not on the page). Steffen's "contact us to schedule a preview" is the fixture |
| **2026-09-25** | **The claim guard never trusts the model's kind tags** (Session 15) | Fact tokens (numbers, money, proper nouns, tools, number words ≥ three) inside **any** non-offer claim must be in the evidence that claim cites; a `question` or `inference` cannot smuggle a place in. Timing tokens (weekdays, times of day, overnight, after hours, …) are refused unless a `prospect_fact` cites evidence stating the same token (published office hours). Offer language is detected in the text, whatever the tag |
| **2026-09-25** | **`no_cited_evidence` is a refusal** (Session 15, small addition beyond `09`'s list) | A draft with no `prospect_fact` or `inference` citing this lead's evidence has no evidence-backed hypothesis — the brief's "no evidence → hold" rule, enforced on the ledger |
| **2026-09-25** | **Operator edits: claims whose span is gone are dropped, and anything new must be covered** (Session 15) | Telegram edits replace the body only. Approval keeps the claims whose span still appears, re-runs the guard on the exact text, and writes the accepted ledger to the touch and into the approval hash. A sentence deleted → accepted; a fact added or altered (Beaumont, 20+ → 25+) → refused with the list, "Edit not applied". A touch with no ledger (pre-U6b, drill scripts) cannot be approved in Telegram |
| **2026-09-25** | **Claim guard failure: one revision retry, then `manual_hold`, no touch** (Session 15) | The refused draft, violations and prompt version are kept on the `claim_guard_hold` lead_event, and the operator gets one alert. Malformed ledger output (schema, non-JSON) is held too. Generic-guard failures keep the existing park (`writer_generic_3x`), so a figure in **no** evidence is still parked by the older number check before the claim guard sees it |
| **2026-09-25** | **Research sources unit (UR), with U15** (operator decision, Session 15) | **Apify only — never the operator's own LinkedIn account for scraping.** Sources: person and company LinkedIn posts, LinkedIn profile, job posts, Google reviews, news / company blog. Each becomes typed evidence with URL + date + excerpt so the claim guard can verify it. **Must land before the first real prospect send.** `09` §UR |
| **2026-09-25** | **LinkedIn channel via HeyReach — buy only when that unit (U18) starts** (operator decision, Session 15) | Actions: profile view, like a recent post, connection request (no note or a short one), message after accept; replies flow back into the engine, and **a reply on any channel stops all channels**. Senders are set per campaign: Amir, Ingrida, or Amir only. Low limits: about 15–20 connection requests per day per account. `09` §U18 |
| **2026-09-25** | **Services catalog in the knowledge library** (operator decision, Session 15) | Services: AI automation, CRM build, email marketing, AI agents / RAG, vibe-coded MVP / product builds. Each carries who it is for, the problems it solves, proof, and approved claims. `09` §U13 |
| **2026-09-25** | **Service matcher** (operator decision, Session 15) | An evidence-backed pain → the best service plus the reason, shown on the Telegram card; offer text only from approved facts. `09` §U16 |
| **2026-09-25** | **Offers with rules** (operator decision, Session 15) | E.g. "free automation build": operator on/off switch, monthly slot cap, minimum fit score, eligible segments. The engine only **proposes** an offer to eligible leads; the operator approves each. `09` §U13 (records) and §U16 (eligibility) |
| **2026-09-25** | **Campaigns can be single-service or general** (operator decision, Session 15) | A single-service campaign (e.g. "vibe coding MVP") has its own ICP, search filters and only that service; a general campaign may match any service. `09` §U14 |
| **2026-09-25** | **Build order: email first-send path → research sources (UR) → LinkedIn (U18)** (operator decision, Session 15) | Recorded in `09` §1. Where U7, UD, U8 and U9 fall relative to UR is **open** — confirm at the next planning session |
| **2026-09-25** | **Follow-ups move to Instantly-owned sequence steps (U6c); `emails/reply` only answers a RECEIVED email (U7)** (operator decision, Session 16). Supersedes the Session 14 `additional_recipients` approach for step ≥ 2 | Live re-test (Session 16): `emails/reply` with `additional_recipients=[lead]` was accepted and threaded, but `GET /emails/01a0d9de…` `to_address_email_list` = `amir@zyndixhq.com,ebadiamirhoseineng+s14b@gmail.com` and the Gmail raw To matched. The spec has no `to` field; `additional_recipients` adds to the default recipient (the replied-to email's sender), it never replaces it, and cc/bcc cannot remove it. Replying to a *received* email is fine because its sender IS the lead. Options weighed: (1) Instantly-owned sequence steps, texts approved up front — **chosen**; (2) cc/bcc on `emails/reply` — cannot remove our mailbox; (3) `emails/reply` for received emails only — kept, for U7; (4) tighter post-send check — kept as a guard, not a fix |
| **2026-09-25** | **The post-send check fails closed when ANY of our own mailboxes is in To/Cc** (operator decision, Session 16; built in U6c) | The Session 14 check only asked whether the lead is in To. In Session 16 the lead was in To next to `amir@zyndixhq.com`, so the engine recorded touch `0b5b2539` as `sent` and ledger accepted 4. A follow-up that shows our own mailbox to the prospect must never count as a clean send |
| **2026-09-25** | **U6c drafting is hybrid** (operator decision, Session 17) | The writer writes step 1 and step 2 (a new angle that must cite evidence) in one call; step 3 is a fixed, versioned "honest close" template (`followup_templates` v1). `attach_pdf` from `cadence_default` v1 is dropped because no such asset exists. The claim guard runs on every step (template mode for step 3). One Telegram approval covers the whole sequence; the hash binds every step's subject, body and delay, plus the sender and signature |
| **2026-09-25** | **Follow-up delays are whole multiples of 7 days (0/7/14); the 24/7 schedule stays** (operator decision, Session 17) | Instantly schedules per campaign only (fixed timezone enum, no per-lead timezone). With a 24/7 schedule and 7-day delays, each follow-up lands on the same weekday and local time as step 1, which the engine placed in the recipient's window, for any timezone, with no schedule writes. Rejected: one business-hours zone (wrong outside the US), a campaign per timezone band (4×k campaigns, a one-to-many migration), 0/4/10 on 24/7 (nights/weekends). Caveats: ±1 h across DST, `email_gap` drift |
| **2026-09-25** | **The U6c drill uses a separate campaign `zx-drill-s17-amir-zyndixhq`** (operator decision, Session 17) | Same mailbox and settings, delays in minutes. The production `zx-sender-*` campaigns are PATCHed once, after the engine drill passes (S22) |
| **2026-09-25** | **A sender pause pauses its campaign first, then DELETEs every in-flight lead** (operator decision, Session 17) | Remaining follow-ups are killed, leads → `manual_hold`. A later resume can never silently continue old sequences. Applies to the bounce auto-pause, `stop_processing_stale` and `recipient_misaddressed` |
| **2026-09-25** | **S18 is a live spike before any U6c engine code** (operator addition, Session 17) | A guarded script, no engine stages: 3 literal steps (empty follow-up subjects, 5 min / 5 min), one alias `ebadiamirhoseineng+s17@gmail.com`. Proves: the API accepts an empty subject; step 2 To = the alias only, no Cc/Bcc; same `thread_id`; Gmail `In-Reply-To` = step 1; text/plain only; `DELETE` stops step 3. `--check` first states the `daily_limit` to set in the UI. Every Instantly write asked separately. Any failed check → stop and bring options before S19 |
| **2026-09-25** | **Per-step evidence freshness** (operator addition, Session 17) | A step's claims pass only if evidence age at approval + that step's cumulative delay ≤ `evidence_policy.max_age_days`: at 30, step 2 (+7 d) needs evidence ≤ 23 d old at approval. A template step citing no evidence is exempt. Checked at draft and again at approval |
| **2026-09-25** | **Blank-email guard and frozen texts** (Session 17 design rule) | Instantly replaces a missing variable with an empty string and does not document skipping the step, so an empty `{{zx_body_N}}` would send a blank email. Enroll refuses `sequence_incomplete` unless every step body is present, approved and non-blank. Lead variables are never PATCHed after enroll; a change means stop and redraft |
| **2026-09-25** | **No campaign step change while the campaign holds any lead** (Session 17 design rule) | Help: adding steps reactivates previously completed leads. `instantly-sender-campaigns.ts --update` refuses unless the campaign is paused and holds 0 leads (`campaign_not_paused` / `campaign_has_leads`). The 2 Completed drill leads in `5392fcac` are deleted (with approval) before its PATCH |
| **2026-09-25** | **Every stop removes the lead from Instantly, confirmed, fail closed** (Session 17 design rule) | `stopSequence`: kill unsent follow-ups → `DELETE /leads/{id}` → confirm `GET` 404 → enrollment `removed`. A 5xx or timeout is uncertain (check first). A failure after one retry → escalated `stop_failed` + the sender's campaign paused. A reconcile sweep re-checks every stopped lead against Instantly |
| **2026-09-25** | **The recipient check runs on every sent email, step 1 included** (Session 17 design rule) | `GET /emails/{id}` after each `email_sent`: To must be exactly [lead], Cc/Bcc empty; any `send_accounts` address or `zyndix.com`/`zyndixhq.com`/`getzyndix.com` address → `recipient_misaddressed` (touch failed, hold, stop, sender campaign paused). The engine's `emails/reply` path for step ≥ 2 is hard-disabled (`followup_engine_send_disabled`) |
| **2026-09-25** | **S18 spike ran on amir@getzyndix.com, not amir@zyndixhq.com** (operator, Session 18) | `--check` at 19:28Z: zyndixhq had sent 2 today against `daily_limit` 1; getzyndix had 0. Both healthy, warmup 100. The operator set getzyndix's Instantly daily limit to 3 in the UI for the spike; it goes back to 1 after. Campaign name therefore `zx-drill-s17-amir-getzyndix` |
| **2026-09-25** | **S18 delete was pre-approved once and fired automatically on step 2** (operator, Session 18) | Step 3 was due ≈ 5–15 min after step 2, shorter than a chat round-trip. `--watch --until-step 2 --then-delete` ran one guarded `DELETE` (alias + drill campaign only) and confirmed it; no other write was automated |
| **2026-09-25** | **S18 webhook sink was a script-local listener, not the engine receiver** (operator, Session 18) | The alias has no engine lead, so the engine receiver would have raised `unmatched_recipient` exceptions. The listener checked the token (401 without it), wrote payloads to a scratchpad JSONL file, no DB |
| **2026-09-25** | **Spike campaign `d598def3` left `completed`, not paused** (operator, Session 18) | It auto-completed at 20:09:38Z with 0 leads and nothing left to send. S22 uses its own campaign |
| **2026-09-26** | **Preflight gets a narrow sequence-hash bridge in S19** (operator decision, Session 19) | A sequence-approved touch carries ONE hash over every step; preflight rebuilds it from all the steps sharing that hash and the ACTIVE `email_sequence` (a changed step, delay, sender or signature → `stale_approval`). No new refusals (`sequence_incomplete`, drift, `provider_daily_limit` stay in S20). Rejected: leave preflight untouched (every sequence-approved step 1 would be `stale_approval` until S20 and `test:traversal` would go red) |
| **2026-09-26** | **✏️ Edit N = approve the sequence with step N replaced** (operator decision, Session 19) | Keeps today's edit semantics: the guard re-runs on the edited step plus freshness on every step, then the whole sequence is approved under a new hash in one fenced write. One edited step per approval; for more, Kill and redraft. Rejected: edits that stay pending (new saved-edit state) |
| **2026-09-26** | **Sequence approval is one DB function, `approve_email_sequence` (0009d)** (Session 19, plan mode) | PostgREST cannot fence N rows all-or-nothing; a filtered UPDATE could approve 2 of 3 steps. The function locks the lead's pending outbound touches and writes only if the named touches are exactly those, all `pending_approval` |
| **2026-09-26** | **`email_sequence` `delay` = the wait after the previous step** (Session 19) | v1 0/7/7 → days 0/7/14, matching Instantly help ("counts from when the previous step was sent"). The production schema refuses any delay that is not whole days in multiples of 7, a step 1 delay ≠ 0, and writer steps after a template step. Instantly's own field means "before the NEXT email" → the S20 mapping (§6) |
| **2026-09-26** | **The approval binds the rendered follow-up; the quote of step 1 is covered by the same hash** (Session 19, resolves the S18 quote row) | The snapshot binds a follow-up's subject as Instantly renders it (`Re: <step-1 subject>`) and every step's composed body; step 1's composed body, which Instantly quotes under each follow-up, is in the same hash. The card says "Instantly adds a quote of step 1 below" on steps ≥ 2 |
| **2026-09-26** | **Step 2's delay counts against evidence freshness, so a sequence needs evidence ≤ 23 days old** (Session 19, consequence of the Session 17 rule) | With `max_age_days` 30 and step 2 at +7 d, evidence 24–30 days old holds the whole draft (`stale_evidence` "step 2: Nd + 7d > 30d"); the U6b 29-day case now holds on step 2. A step 3 template citing nothing is exempt |
| **2026-09-26** | **S20 absorbs `step2_repeats_step1` and `writer_prompt_email` v11; not the general backlog** (operator, Session 19) | From the live v10 draft (`07` Session 19): step 2 repeated step 1's observation and generalised. (a) step 2 must cite ≥ 1 evidence id step 1 does not → retry once → hold `step2_repeats_step1`; (b) v11: no visitor-behaviour claims unless evidence states them, no "usually/most" statements, step 2 uses a different evidence item than step 1. Recorded in `09` §U6c "S20 scope additions" |
| **2026-09-26** | **A Telegram edit that drops the compliance footer gets it re-appended** (operator decision, Session 20 plan mode) | An edit replaces the whole body. If the edited body does not end with the active `compliance_footer`, approval appends it (`appendComplianceFooter`) before the sign-off check, guard, snapshot and RPC, so the hash binds it and the APPROVED texts show it. An edit that keeps the footer is unchanged (no double footer). Rejected: refuse the edit (more operator round-trips for a mechanical fix) |
| **2026-09-26** | **The last Instantly step's `delay` repeats the last engine step's delay and unit** (Session 20) | Engine step N+1's delay goes on Instantly step N (`lib/sending/campaign-sequence.ts`); the last Instantly step has no next email. Help discourages 0, the value never gates a send, and one unit keeps the drift diff uniform. v1 0/7/7 → Instantly 7/7/7 days. S22 confirms the mapping live with distinct delays |
| **2026-09-26** | **A step 1 approved on its own (pre-U6c single-touch snapshot) is refused `sequence_incomplete` (`not_sequence_approved`)** (Session 20) | Enrolling it into a 3-step campaign would send blank follow-ups. Fail closed; Kill and redraft |
| **2026-09-26** | **Instantly's `daily_limit` gates the enroll; the engine quota = min(ramp, `daily_limit`)** (Session 20, 09 §U6c scope 5) | Preflight refuses `provider_daily_limit` (deferrable → the first window after the next UTC midnight) when `sent_today + follow-ups due today + 1 > daily_limit`. `sent_today` = `GET /accounts/analytics/daily` for the UTC date (the spec does not state the analytics timezone; no row = 0). Follow-ups due = every still-`approved` step ≥ 2 whose step 1 went out from this mailbox, due by the end of today UTC (step 1 `sent_at` + the delays bound in its snapshot); overdue ones count. An unreadable limit or count → `sender_unhealthy` `provider_daily_unread` (hold, never assumed room). The reservation quota passes min(ramp, `daily_limit`); the ledger day quota only ever lowers |
| **2026-09-26** | **The enroll records an `instantly_enrollments` row** (Session 20) | Written after the provider accepted the lead (and when reconcile finds an uncertain enroll): `active`, `sequence_hash` = the approval hash, `steps_total`, `provider_lead_id`, campaign. The live-lead unique index turns a second row into an error. S21's `stopSequence` and reconcile sweep read it |
| **2026-09-26** | **`--update` accepts a paused or never-run draft campaign; `--update --apply` needs `--only <mailbox>`** (Session 20) | Status 0 (draft) and 2 (paused) cannot send; anything else is `campaign_not_paused`. Any lead in the campaign (`leads/list`, limit 1) is `campaign_has_leads`. PATCHes go one campaign at a time (09 §U6c S22), each re-read and diffed after |
| **2026-09-26** | **`getEmail` shares the 20 req/min `GET /emails` limiter** (Session 20) | The spec states the 20/min limit only for the list endpoint; `GET /emails/{id}` has no stated special limit. Sharing the limiter is conservative; S21's recipient check is the caller |
| **2026-09-26** | **E1–E6 run inside `test:send`, not a new `test:enroll` suite** (Session 20, deviation from the plan text) | The send stage's DB harness (fixtures, mocks, scoped cleanup, before/after counts) already exists there; every step-1 fixture is now a 3-step sequence approval, and the old step-2 reply/anchor/misaddressed cases became E6 |

---

## 6. Issues / blockers log

| Date | Issue | Status | Resolution |
|---|---|---|---|
| 2026-09-26 | **A held draft's writer tokens and cost are not counted** (Session 20, live v11 run). `runDraftStage` adds `tokens_used`/`est_cost_usd` only when a draft passes, so the run with 2 real writer calls reported `est_cost_usd: 0`. Spend reporting (and any future budget check) undercounts every hold and retry | 🟨 open — low, before U9's daily digest | Carry tokens/cost on the failure result of `writeSequenceWithGuard` and add them to the summary (and to the hold `lead_event`). A small fix outside S20's scope |
| 2026-09-26 | **The analytics timezone for `sent_today` is unstated** (Session 20, spec read). `GET /api/v2/accounts/analytics/daily` returns `date` as YYYY-MM-DD with no timezone; the engine queries the UTC date. If Instantly counts in another zone, `sent_today` can be off near midnight | 🟨 open — low | At current volume (`daily_limit` 1–3) the margin is one email. S22 can compare the analytics row with the drill's send times |
| 2026-09-26 | **The live campaigns are still single-step** (Session 20, read-only `--verify`): all four `zx-sender-*` report `campaign has 1 step(s), the sequence has 3` and `step 1: delay 0 days (want 7 days)`. So every step-1 enroll is refused `campaign_sequence_drift` until they are PATCHed. `--update` dry run: amir@getzyndix, ingrida@getzyndix, ingrida@zyndixhq (drafts, 0 leads) → PLAN; `5392fcac` amir@zyndixhq (paused) → **`campaign_has_leads`** (the 2 Completed drill leads, row below) | 🟨 open — by design until S22 | S22: engine drill on its own campaign, then delete the 2 drill leads and PATCH each production campaign one at a time (`--update --apply --only <mailbox>`), each re-verified |
| 2026-09-25 | **Instantly reactivates completed leads when steps are added** (help: "previously completed leads will be reactivated"). Campaign `5392fcac` (amir@zyndixhq.com) holds 2 Completed drill leads (`01a0d900…`, `01a0d9d8…`). Adding steps whose body is an empty `{{zx_body_2}}` would send them a blank step 2 | 🟨 **open** — no step change has been made | U6c rule (§5): `--update` refuses while the campaign holds any lead. The 2 drill leads are deleted with operator approval in S22, before the PATCH |
| 2026-09-25 | **An Instantly follow-up step carries a quoted copy of the previous step** (S18, Gmail raw of step 2: "On Fri … wrote: > …" under the step-2 text). What the lead receives is the approved step-2 text + the quoted step 1 | ✅ **resolved Session 19 — by design** | The snapshot binds every step's composed body and the rendered follow-up subject; step 1's composed body (what Instantly quotes) is in the same hash, so the quote only repeats approved text. The card states "Instantly adds a quote of step 1 below" on steps ≥ 2 (§5) |
| 2026-09-26 | **The claim guard's semantic gap is still covered only by operator review** (Session 19, operator). The live writer v10 draft passed the guard while step 1 said "they fill out the form" (E1: "rather than a routed form") and added "the next showing", and step 2 repeated step 1's observation and generalised ("usually where buyer interest goes quiet"). Claims with no fact tokens are checked only for citation and contradiction (interim gap (a) in the U6b row below) | 🟨 **open** — by design until U15/U17; **narrowed Session 20** | Until U15 (typed, verbatim evidence) and U17 (the full guard), only the operator's review of each card catches these. Session 20: `step2_repeats_step1` is built (draft retry → hold, re-checked at approval, **tested locally**); `writer_prompt_email` v11 is **applied**. **Its one live draft (synthetic, `07` Session 20) still showed the gap:** no "usually/most" and no contradiction, but step 2 narrated visitor behaviour ("a lead can view a listing, send a message, and get nothing back"), step 1 cited both evidence items, leaving step 2 nothing new, and "the CRM is receiving data" is not in the evidence. The claim guard held it (other reasons). The prompt reduces the gap; it does not close it |
| 2026-09-26 | **`scripts/test-validation.ts` fails 1 case on `main`**: "writer 90-word body passes" (its fixture has no `claims`, which `writerOutputSchema` requires since U6b, Session 15). Same result with this session's changes stashed | ✅ **resolved Session 20** | The fixture carries one valid claim; `test-validation` 16/16 |
| 2026-09-26 | **`cadence_default` v1 (0/3/7/14, attach_pdf) still seeds `sequences`/`sequence_steps`**, while the U6c draft stage uses `email_sequence` v1 (0/7/14). Touches reference the `v1_default` sequence id; nothing sends from `sequence_steps` | 🟨 **open** — low | Harmless now (no runtime reader of `sequence_steps.wait_days`). Align or retire `cadence_default` when U14 (per-campaign cadences) lands |
| 2026-09-26 | **A Telegram edit replaces the step body verbatim, so an edit that drops the compliance footer is approved without it** (pre-existing since U5; the guard strips the footer before checking) | ✅ **resolved Session 20** — tested locally | Operator decision: re-append. `bindSequenceApproval` appends the active footer to an edited body that lacks it, before the guard and the hash; `test:sequence` "footer: …" (approved body = edit + footer, once; snapshot binds it; APPROVED text shows it) |
| 2026-09-26 | **Engine and Instantly define a step's `delay` differently** (Session 19, operator). Engine `email_sequence` `delay` = the wait **after the previous step** (v1 0/7/7 → days 0/7/14). Instantly step `delay` = the wait **before the NEXT email** (spec) | 🟨 **open** — **built Session 20 (tested locally, M1 10/10)**, S22 confirms it | S20 maps engine step N's delay onto Instantly step N−1: Instantly step 1 gets engine step 2's delay, step 2 gets step 3's; the last Instantly step has no next email (S20 picks and records its value). `diffCampaign` / `campaign_sequence_drift` compare through the same mapping. Pinned by pure test M1 (`09` §U6c DoD). S22 uses different per-step delays to confirm it live, which also closes the row below |
| 2026-09-25 | **Which step's `delay` sets the gap before step 2 is still unproven** (S18). All 3 steps had `delay: 5` minutes; step 2 went out 15 min after step 1 (19:39:22 → 19:54:23Z), i.e. 5 min + `email_gap` 10, which fits both readings (spec: "before sending the NEXT email") | 🟨 **open** — S19/S22 | Production intends 0/7/14 days. The S22 drill uses distinct per-step delays (e.g. step 1 = 5, step 2 = 20 min) so the observed gap names the field. S19 builds to the spec reading (step N's `delay` = the wait before step N+1) and S22 confirms or corrects it |
| 2026-09-25 | **Unproven provider mechanics for U6c** (Session 17, docs): whether the API accepts an empty follow-up `subject` (the spec marks it required); who a follow-up step is addressed to; `DELETE /leads/{id}` timing; whether emails stay readable after a lead delete; the `0_0_0` step format | ✅ **resolved Session 18** — S18 spike, **verified with provider** | Empty subject accepted (HTTP 200, stored `""`, sent as "Re: <step 1 subject>"). Step 2 addressed to the lead only (API + Gmail raw). `DELETE` 13 s after the step-2 webhook stopped step 3 (none by 20:30Z; campaign auto-completed 20:09:38Z). Emails stay readable after the delete. API `step` = `"<seq>_<step>_<variant>"`, 0-indexed (`0_0_0`, `0_1_0`); webhook `step` is 1-indexed. Evidence in `07` Session 18 |
| 2026-09-25 | **The post-send recipient check misses a follow-up addressed to BOTH our mailbox and the lead** (Session 16 live re-test). `send/core.ts` fails closed (`reply_misaddressed`) only when the lead is missing from `to_address_email_list`. Step 2 had To = `amir@zyndixhq.com,ebadiamirhoseineng+s14b@gmail.com`, so the engine recorded touch `0b5b2539` `sent`, outbox `accepted`, ledger accepted 4 | 🟨 **open** — the `emails/reply` send path (and its `reply_misaddressed` check) is **removed in Session 20**; step ≥ 2 is refused `followup_engine_send_disabled` | Operator decision (§5): fail closed when any of our own mailboxes is in To/Cc. The post-send recipient check on every `email_sent` (`getEmail`, adapter built Session 20) is S21 |
| 2026-09-25 | **Step-2 raw is `multipart/alternative` (text/plain + text/html)** (Session 16, Gmail raw, SPF/DKIM/DMARC pass). The send stage passes `body: {html, text}` to `emails/reply`, so this still does not show whether the endpoint honours a text-only send | 🟨 **open** — report only | Moot for follow-ups once U6c sends them as campaign steps (the campaign's own `text_only` settings apply). Revisit for U7 replies |
| 2026-09-25 | **Engine send quota and Instantly `daily_limit` are not synced** (Session 16 prep). `drill-u6.ts --check` read the sender amir@zyndixhq.com: Instantly account `daily_limit=1`, analytics `sent_today=1`. The engine's `capacity_ledger` for the same day says `quota=15 used=2 accepted=2`. Nothing syncs the engine ramp quota with the account's `daily_limit`, so the engine could reserve and dispatch step-1 enrolls that Instantly will not send that day (they wait in the campaign, and the engine records them `accepted`) | ✅ **resolved Session 20** — tested locally | Step-1 preflight reads `daily_limit` and today's `sent` and refuses `provider_daily_limit` (deferrable) when sent + follow-ups due today + 1 > limit; the engine quota = min(ramp, `daily_limit`) (§5). `test:send` E5 |
| 2026-09-25 | **`emails/reply` follow-ups are not capped by Instantly's `daily_limit`** (Session 16 prep). Evidence: in Session 14, step 2 went out via `emails/reply` after step 1 had used the 1/1 budget. Instantly accepted it (`GET /emails/01a0d90b…` shows it), and today's `accounts/analytics/daily` still reports `sent=1` for that sender and day. So the engine's `capacity_ledger` is the **only** cap on follow-ups | 🟨 **mostly moot — Session 20** | The engine no longer sends any step ≥ 2 (`followup_engine_send_disabled`). Follow-ups are campaign steps, which count against `daily_limit`, and preflight reserves room for them. Still open: counting each Instantly-sent follow-up in the engine ledger (`record_provider_send` on `email_sent`) is S21 |
| 2026-09-25 | **Claim guard interim gaps** (U6b, Session 15) — the guard is token-based, not semantic. (a) A claim with no fact tokens is checked only for citation and contradiction: "no routing and no auto-response" passes if it cites an item, whatever that item says (seen in the live v9 draft, where the fixture did support it). (b) Lowercase place names ("houston" in a lowercase subject) are not detected; v9 tells the writer to capitalise. (c) Spelled-out numbers below three are not tokens. (d) Contradictions cover only chat / booking / contact form. (e) Evidence age is one date per lead (latest non-error fetch) | 🟨 open — by design until U15/U17 | The operator still reads every draft; the Telegram card now shows each claim with its evidence id, excerpt and fetch date. U15 (typed, verbatim, dated evidence) and U17 (the full guard) close (a), (d) and (e) |
| 2026-09-25 | **⛔ BLOCKING: an `emails/reply` follow-up goes to the SENDER, not the lead** (U6 drill, Session 14). Step 2 (`POST /api/v2/emails/reply`, `reply_to_uuid` = step 1 `01a0d902…`) was accepted and threaded correctly (`In-Reply-To`/`References` = `<01a0d902-2458-737a-b51b-1257f2696fc2@zyndixhq.com>`, one conversation), but `GET /api/v2/emails/01a0d90b…` shows `to_address_email_list: "amir@zyndixhq.com"` — the mailbox itself. Official spec: the default recipient is "the sender of the email being replied to"; the endpoint has no `to` field, only `additional_recipients`, `cc_address_email_list`, `bcc_address_email_list`. Replying to our own step 1 therefore addresses ourselves. The engine recorded touch `0af31817` as `sent` and ledger accepted = 2 although the lead never received it | ⛔ **open — blocks every step ≥ 2 and the 🚩** · Session 16 live re-test **failed** (our mailbox still in To next to the lead) · stays open until U6c passes a live drill | Root cause: the send stage's use of the endpoint (`stages/send/core.ts` → `replyToEmail`), not the drill. **Fix (operator-approved, Session 14):** `additional_recipients=[lead.email]` on every step ≥ 2 + fail-closed `reply_misaddressed` check on the accepted email's `to_address_email_list` (`test:send` 78/78). **Re-test Monday** in an open window with a NEW drill lead `drill:s14b`; if our own mailbox stays visible in To next to the lead, **stop** and bring options (fallback preference: Instantly-owned sequence steps, all texts approved up front). Misaddressed touch `0af31817` left as recorded (`sent`), with a `drill_step2_misaddressed` lead_event. **Session 16 re-test (alias `ebadiamirhoseineng+s14b@gmail.com`, lead `387b413d`):** step 2 `01a0d9de-1199-7177-8f6b-8f309a1ed537` → `to_address_email_list` `amir@zyndixhq.com,ebadiamirhoseineng+s14b@gmail.com`, cc/bcc empty, same `thread_id` `53-Q0Ei8ykDh96JFtFoilZEhYx`; Gmail raw: `To: amir@zyndixhq.com, ebadiamirhoseineng+s14b@gmail.com`, no Cc, `In-Reply-To`/`References` = step 1's Message-ID, Inbox, one conversation. **STOP → decision (§5): follow-ups move to Instantly-owned sequence steps (U6c).** Touch `0b5b2539` left as recorded (`sent`) |
| 2026-08-23 | **`scripts/test-draft.ts` destroys real data.** Selected the oldest real `pending_approval`/`parked` leads, `DELETE`d their touches and force-wrote `leads.state` directly, bypassing `lib/state.ts` and writing no `lead_events` | ✅ **resolved 2026-09-21** | Rewritten against synthetic fixtures with scoped cleanup, all transitions through `lib/state.ts`, an abort guard if any non-fixture lead is in `drafting`, and a before/after non-fixture row-count assertion. 32/32 pass; counts identical |
| 2026-08-23 | `scripts/ping.ts` does not exist, but `CLAUDE.md` and step 1's DoD both name it | ✅ **resolved 2026-09-21** | Written as a **read-only** round-trip. Migration `0001` ends with `drop table if exists _ping`, so the original write-a-row DoD was unrunnable against the real schema |
| 2026-08-23 | `ANTHROPIC_API_KEY` returned `401` | ✅ resolved 2026-08-23 | Key replaced in `.env.local`. **Still to do: replace it in the Vercel env** — the fix is local only |
| 2026-09-21 | **`source_cursors` (migration `0004`) has no RLS and no `updated_at` trigger**, unlike all 16 tables in `0001`. Not exploitable today — `anon`/`authenticated` are ungranted — but it breaks the pattern | ✅ **resolved 2026-09-24** | `0005` applied. `trg_updated_at` proven firing by `test-u1-auth.ts` group 4b. RLS proven by the `relrowsecurity` query in the migration's trailer, run by the operator 2026-09-24: `app_users`, `leads` and `source_cursors` all `true` (`07` Session 7) |
| 2026-09-21 | `STEP-11-RUNBOOK.md` claimed the `zyndix.com` guard was "already enforced by a code guard in `stages/send.ts`". No such file exists | ✅ resolved 2026-09-21 | Runbook corrected. The guard arrives at **U5** and that unit's DoD verifies it |
| 2026-08-23 | `TELEGRAM_WEBHOOK_SECRET` unset → `/api/webhooks/telegram` 500s | 🟨 open | Scheduled into **U9**. `telegram-poll.ts` covers approvals until then |
| 2026-08-23 | `source_cursors` not documented in `02-database-schema.md` | 🟨 open | Docs 01–05 now carry a drift banner naming it. Full reconciliation is **U23** |
| 2026-07-13 | **JS-rendered sites returned no pages to the crawler.** `fantasticfrank.co` returned zero pages in cheerio mode | 🟨 mitigated, unverified | Templates bumped to v3 with `crawlerType: playwright:adaptive`. **Never re-tested against that site** — do not assume any site is uncrawlable |
| 2026-07-13 | **Qualification rate ≈ 20%** (1 of 5), implying ~125 leads/week sourced to hit 25 contacts/week | 🟨 open | Monitor. Do **not** widen the ICP to close the gap — a vaguer ICP means a vaguer message |
| 2026-07-13 | **`QUALIFY_MIN_SCORE` unsettled.** Currently 50; Stephan Group scored 52, so 60 would have qualified nobody | 🟨 open | Needs more scored leads. Do not change it to raise throughput |
| 2026-09-21 | `test-draft.ts` ✏️ edit and ❌ kill paths undemonstrated | 🟨 open | Each needs its own fixture — an approved lead cannot transition to `parked`. Backlogged in `09` §5 |
| 2026-09-21 | **U1's DoD is partly blocked on operator steps.** `app_users` store checks need `0005` applied; the HTTP checks need `SUPABASE_ANON_KEY`, `DASHBOARD_ALLOWED_EMAILS` and the Supabase email provider | ✅ **resolved 2026-09-24** | All four steps done. 48/48 |
| 2026-09-24 | **Dashboard auth is proven on localhost only.** `SUPABASE_ANON_KEY` and `DASHBOARD_ALLOWED_EMAILS` are confirmed in `.env.local`; their presence in the Vercel env, and the production callback URL in Supabase's redirect allow-list, are not verifiable from inside the repo | 🟨 open | Check both before any dashboard deploy. Same class of gap as the Anthropic key still dead in Vercel |
| 2026-09-21 | **`pnpm lint` fails on `main` — 17 errors, 4 warnings**, all `no-explicit-any` in pre-existing `scripts/*.ts` plus unused-var warnings in `src/lib`. Identical counts before and after U1 | 🟨 open, pre-existing | Not touched by U1 (scope discipline). `pnpm build` and `tsc --noEmit` are clean. Worth a dedicated cleanup session — backlogged in `09` §5 |
| 2026-09-21 | **`0002_transition_lead.sql` is `security definer` with no `set search_path`.** Supabase's linter flags this as `function_search_path_mutable` | 🟨 open | Out of U1's scope; fixing it means a new migration replacing the function. Backlogged in `09` §5 |
| 2026-09-25 | **Same mailbox names on both sending domains** (`amir@`, `ingrida@` on `zyndixhq.com` and `getzyndix.com`) → a lead must keep one `send_account` for its whole sequence, never rotating mid-sequence | ✅ **resolved 2026-09-25 (U5)** | `leads.send_account_id` binding (conditional write at first send), one draft campaign per mailbox, follow-ups via `emails/reply` from the bound `eaccount`, and preflight `sender_mismatch` before any reservation. Proven in `test-u5-send.ts` |
| 2026-09-25 | **`getzyndix.com` "You're not fully authenticated" on mail-tester**, despite 9.6/10 on both mailboxes. DKIM had been enabled minutes before the test. `dig`: both DKIM selectors resolve, but `_dmarc.getzyndix.com` was NXDOMAIN | ✅ **resolved 2026-09-25 (Session 12)** — re-test passed (operator-reported) | Session 11: exactly one `_dmarc` TXT (dmarcly, `p=none`) on public and authoritative resolvers. Still to do: re-run mail-tester on both getzyndix mailboxes. Not passed until the re-test says authenticated |
| 2026-09-25 | **Instantly account `daily_limit` is 4, not the intended 1.** Session 10 read 30; the operator then set the daily campaign limit to 1 on all four as a pre-first-send safety net; the Session 11 API read shows **4** on every account | ✅ **resolved Session 12** — API re-read: `daily_limit 1` on all four | Check the value in the Instantly UI (the field may round, or a different setting was changed). Changing it is a live-settings write, so the engine did not touch it. With draft-only campaigns and no leads nothing can send, and the engine ledger (15→30 ramp) caps sends regardless. Re-read with `live-instantly.ts --accounts` |
| 2026-09-25 | **`enable_slow_ramp` differs by domain**: true on the zyndixhq accounts, false on the getzyndix ones (Session 11 read) | ✅ **resolved Session 12** — `false` on all four (API re-read) | Probably a default of each connection path. Harmless while nothing sends; make it consistent before U6 if intended |
| 2026-09-25 | **`link_tracking` is not echoed by Instantly.** The sender campaigns were created with `link_tracking:false`, but `GET /api/v2/campaigns/{id}` omits the field (as it does `stop_on_auto_reply:false`), while the required `open_tracking:false` is echoed | ✅ **resolved Session 12** — link tracking off confirmed in the UI (operator) | Inferred as "false-valued optional flags are omitted". `--verify` flags drift only if it is ever `true`, and prints "not echoed". Confirm once in the Instantly UI for one campaign |
| 2026-09-25 | **Threading headers and `emails/reply` access were unproven.** The spec documents `POST /api/v2/emails/reply` (scope `emails:create`, lists a 402 response) and a shared `thread_id`, but not the RFC `In-Reply-To`/`References` headers or whether Growth allows the endpoint | ✅ **resolved Session 14** — Growth accepted `emails/reply` (no 402). Operator's Gmail "Show original" on step 2: `In-Reply-To` and `References` = `<01a0d902-2458-737a-b51b-1257f2696fc2@zyndixhq.com>` (step 1's Message-ID), same conversation | Threading works. The **recipient** did not — see the ⛔ blocker at the top of this table |
| 2026-09-25 | **Every current lead would be held `timezone_unknown`.** All 34 companies are `country = 'US'` with null timezones, and all 34 leads have null timezones; the US has many zones, so no country fallback applies | 🟨 **mitigated Session 12** — the 4 send candidates now have a timezone from the HQ state (Apollo); 30 stay held `state_missing`. The permanent fix is the backlog row below | Correct behaviour under the operator's rule, not a bug. Before the first US prospect send, set `leads.timezone`, for example from Apollo's person or organization location data (U15 evidence model) |
| 2026-09-25 | **The verify stage's `isSuppressed` treats any domain match as suppressed** — including the `domain` it writes beside an invalid email, so one invalid address suppresses its colleagues at the source stage | ✅ **resolved Session 12 (U6)** — verify **and** source (same bug found there) now delegate to `checkSuppression`, and lookups fail closed | Out of U5's scope (verify stage). U5's own pre-send check uses person-level versus company-wide scope. Worth aligning in U6, which owns suppression |
| 2026-09-25 | **Webhooks on Growth are unproven.** A third-party source says webhooks need Hypergrowth; the official docs name no tier. The event-types endpoint answered on Growth (18 types). Creating a webhook is a write and was not attempted | ✅ **resolved Session 12** — created on Growth (operator-approved), test delivery 200 through a tunnel, stored and processed, then deleted | If creation is refused on Growth, U6 needs either a plan change or a polling fallback. Also: the live event list differs from both the spec and the guide (has `custom_label_any_positive/negative`; no `auto_reply_received`, no `lead_no_show`) — U6 must use the live list |
| 2026-09-25 | **Two legacy Instantly webhooks** from 2025-10-15 (`email_sent`, `reply_received` → `hook.eu1.make.com`, no auth header), from the operator's old Make.com campaign. Once sending starts, reply text would also reach that scenario | ✅ **resolved Session 13** — deleted with operator approval (`0199e630-78d2-…`, `0199e630-78cf-…`); `--list` after → `webhooks: 0` | The drill gate is cleared. The drill creates its own tunnel webhook |
| 2026-09-25 | **Sourcing and verify discard location data.** HQ state, city and person location are never stored, so timezones need extra Apollo credits | 🟨 open — backlog (`09` §5) | Sourcing/verify must store the person's city/state/country and the company's `hq_state`/`hq_city` from the Apollo data already fetched; the person's own location wins over HQ when both exist (operator, Session 12) |
| 2026-09-25 | **The 4 existing drafts still carry the v2 footer ("— Amir")**, and Steffen's approved touch was hashed under the U5 snapshot | ✅ **closed Session 13** — redrafted under v8, then **killed after the claim audit** (the row below); the leads are parked | `redraft-drafts.ts --apply` (operator-approved, 5 Anthropic calls, $0.035): old touches `killed` (kept), leads `→ drafting → pending_approval`, 4 new v8 drafts, none signs itself, 4 Telegram cards to the operator. Steffen is `pending_approval` again. Approval only binds; nothing sends before U9 |
| 2026-09-25 | **All current evidence is stale, and the v8 drafts made unsupported claims.** Every qualified lead's evidence comes from one crawl on **2026-07-13** (74 days old at the audit). The audit of the 4 v8 drafts (`07` Session 13) found: invented timings in all 4 ("9pm on a Saturday", "Tuesday evening"); an invented place (REBG "Beaumont"); "I've mapped out … fixes" with no asset (REBG, Stride); a claim contradicted by the site (REBG says "try the chat icon" while the body says nothing answers); Steffen's "running 9+ concurrent auctions", which all ended July 5 – August 23; "through a contact form" contradicted by Steffen's own E3 and absent from Stride's page; a failed tech crawl used as evidence (Stride E3, prompt v1); and a misquoted phrase in Steffen's E3. The evidence items have no URL, date, verbatim excerpt or observed/inferred label | 🟥 open — **blocks prospect sends** | Operator decision: the 4 v8 touches → `killed` (rows kept); the 4 leads `pending_approval → parked` via `lib/state`, event `parked`, `park_reason stale_evidence_2026-07-13`. Next: U6b (claim guard). Re-crawl and re-qualify are a separate, costed decision. The 10 `qualifying` leads rest on the same crawl date (2026-07-13), so treat their evidence as stale too |
| 2026-09-25 | **Webhook `email_id` vs `GET /emails` `id` was unproven equal** | ✅ **resolved Session 14 — equal** | Observed live: `email_sent.email_id` = `GET /emails` sent `id` (`01a0d902…`) and `reply_received.email_id` = `GET /emails` received `id` (`01a0d953…`). The polled reply replayed through the processor returned `duplicate_reply` with no second inbound touch. Side finding: `runReplyPoll` never polls a lead already `replied` (by design — its window covers awaiting leads only); recorded in `09` §5 |
| 2026-09-25 | **The 20 req/min spacing on `GET /emails` is per process** | 🟨 open — low, U9 | `send.reconcile` and the reply poll share one limiter per process; U9 runs one worker. A second worker could exceed the limit; a 429 then only truncates a poll run (resumes next tick) |
| 2026-09-25 | **`auto_reply_received` is not in the live event list** (Session 10 read), so auto-replies may arrive as `reply_received` | 🟨 open — partly observed Session 14 | The live human `reply_received` payload has **no `is_auto_reply` field** at all (keys: `campaign_id, campaign_name, companyName, email, email_account, email_id, event_type, firstName, is_first, lastName, lead_email, reply_html, reply_subject, reply_text, reply_text_snippet, step, timestamp, unibox_url, variant, workspace` + the lead's custom variables `zx_body, zx_subject, zx_touch_id`). So on the webhook path auto-reply detection rests on the subject patterns only (the poll path has `is_auto_reply`). A real auto-reply payload is still unseen |
| 2026-09-25 | **A local `~/.cloudflared/config.yml` (a named tunnel from July) hijacks quick tunnels** — its ingress rules apply and the quick hostname returns an empty 404 | ✅ worked around | Run the quick tunnel with an empty config: `cloudflared --config <empty.yml> tunnel --url http://localhost:<port>`. The operator's config was not modified |
| 2026-09-25 | **Drill step 1 landed in Gmail Spam** (amir@zyndixhq.com, warmup day 2; reason not captured; operator marked "Not spam") | 🟨 open — **gates prospect sends** | No prospect send until warmup completes **and** an inbox-placement test passes (§5, Session 14). Step 2 never reached the operator's Gmail (misaddressed) |
| 2026-09-25 | **Step 2's raw email had a `text/html` part although the campaign is text-only** | 🟨 open — cause known, behaviour of the endpoint undetermined | The send stage itself passes `body: {html, text}` to `emails/reply` (the spec says line breaks need `html` with `<br/>`), so the HTML part is ours, not proof that `emails/reply` ignores `text_only`. `text_only` is a campaign setting; `emails/reply` has no such flag in the spec. Test in the Monday re-test by comparing step 1's raw (campaign, text-only) with the follow-up's; decide then whether to send `text` only |
| 2026-09-25 | **Cloudflare quick tunnels can drop mid-drill** — the Session 14 tunnel failed at 16:03 UTC with repeated "control stream encountered a failure" | ✅ worked around | Detected by an unauthenticated probe (HTTP 000) before the reply step. A new tunnel and a delete + re-create of the webhook (operator-approved) fixed it. Before any reply step, probe the tunnel; a named tunnel or the deployed URL is the durable answer (U9 deploy) |
| 2026-09-25 | **Step 1's touch never gets `provider_message_id`** | 🟨 open — backlog | Enroll returns only the Instantly lead id; the `email_sent` webhook writes the email id to `outbox.provider_email_id` (the thread anchor), not to the touch. Observed live in the drill. Harmless for sending; fix with U9/U10 dashboards (`09` §5) |
| 2026-09-25 | **The proxy calls `supabase.auth.getUser()` on every request, webhooks included** | 🟨 open — low | Harmless (only `/dashboard` is gated), but it is an auth round trip per webhook delivery. Consider excluding `/api/webhooks` from the matcher at deploy (U23) |
| 2026-09-25 | No tracking CNAME (`track.`) on either sending domain | ✅ **closed 2026-09-25 — not needed** | Operator decision (§5): no custom tracking domain; open and link tracking off |
| 2026-09-21 | **`@supabase/ssr` required a `supabase-js` bump**, 2.110.1 → 2.116.0, to satisfy its `^2.114.0` peer | ✅ resolved 2026-09-21 | Minor bump inside the same major. `tsc`, `pnpm build`, `test-validation` (16/16) and `test-state` re-run clean afterwards |

---

## 7. Settings version log (mirror of DB — verified live 2026-08-23)

| Key | Active version | Last change | Note |
|---|---|---|---|
| icp_rubric | v1 | 2026-07-13 | seed content unchanged |
| segments | v3 | 2026-07-13 | expanded `exclude_keywords`, dropped president title; `lt-events` present, `active: false` |
| qualifier_prompt | v4 | 2026-07-13 | contradiction rule: discard contested evidence |
| writer_prompt_email | **v11** | 2026-09-26 | **v11 (Session 20, operator-approved, `update-writer-prompt-v11.ts --apply`):** v10 + a "NO BEHAVIOUR OR GENERIC CLAIMS" block (behaviour only if evidenced; no usually/most/often/typically/many/tend to; no detail the evidence lacks, never its opposite); step 2 MUST cite an evidence item step 1 does not; v10's "they fill out the form and wait" example removed. Live check: 1 synthetic draft, held by the claim guard (`07` Session 20). **v10 (Session 19, operator-approved):** STEP VARIANTS replaced by a SEQUENCE block (steps 1–2; step 2 in the same thread, no subject, ≤70 words, a new angle citing evidence, any offer = the approved line), output `{steps:[{step_no, subject?, body, claims}]}`. v9: v8 + CLAIMS section and `{subject, body, claims}` output; evidence ids E1…En; sentence 2–3 is a mechanism, never a time or delay; no weekdays/times, no "I've prepared / mapped out"; the approved offer line verbatim (Session 15, operator-approved, built from the active v8 by `update-writer-prompt-v9-claims.ts`) |
| writer_prompt_linkedin | v1 | 2026-07-13 | seed; consumed by nothing until U18 |
| reply_classifier_prompt | v1 | 2026-07-13 | seed; consumed by nothing until U7 |
| cadence_default | v1 | 2026-07-13 | 0/3/7/14, stop on reply |
| capacity_defaults | v1 | 2026-07-13 | 15→30/day ramp; auto-pause at 3% bounce / 1 complaint. `email_inbox` read by `rampQuota()` (U3); the send stage (U5) is the first runtime caller |
| send_windows | v1 | 2026-07-13 | Tue–Thu priority, 08:30–11:00 local. Read by `nextSendWindow()` (U3); U5 is the first runtime caller. Schema gained optional `priority_lookahead_hours` (default 48) in U3 — v1 unchanged, no new version written |
| send_policy | **v2** | 2026-09-25 | v1 (U5): `verification_max_age_days 90`, `allow_catch_all false`, `min_warmup_score 80`, `duplicate_company_window_days 30`. **v2 (Session 12, operator-approved): + `assignable_senders` = both amir@ mailboxes**, read at Telegram approval |
| apify_actor_templates | v3 | 2026-07-13 | site crawler → `playwright:adaptive` |
| compliance_footer | **v3** | 2026-09-25 | Address + "Reply STOP" line only; the "— Amir" sign-off is gone because the mailbox signature goes right before the footer (Session 12, operator-approved) |
| cta_variants | **v3** | 2026-09-25 | active `reply` = exactly "Happy to write up what I'd change, if that's useful." + `approved_lines` (Session 15, operator decision). v2 was an instruction with example lines |
| evidence_policy | **v1** | 2026-09-25 | **new key** (U6b): `max_age_days 30` → `stale_evidence` |
| email_sequence | **v1** | 2026-09-26 | **new key** (U6c, Session 19): 3 steps, `delay` 0/7/7 days after the previous step (days 0/7/14); steps 1–2 `writer`, step 3 `template` |
| followup_templates | **v1** | 2026-09-26 | **new key** (U6c, Session 19): step 3 `honest_close`, the operator's exact text with `{first_name}` |
| proof_points | v1 | 2026-07-13 | `us-realestate` null; `lt-events` verified |

Session 19's three (`email_sequence` v1, `followup_templates` v1, writer v10) and Session 20's writer v11 make **39 rows across 17 keys**. `linkedin_senders` is agreed but not yet created. `operations_pause` arrives at U9. 28 settings rows across 13 keys, confirmed by `ping.ts` 2026-09-21; `send_policy` v1 (2026-09-25) makes 29 rows across 14 keys; Session 12's three new versions make **32 rows across 14 keys**; Session 15's three (writer v9, cta v3, the new `evidence_policy` v1) make **35 rows across 15 keys**.

---

## 8. Weekly metrics snapshot (fill from digest, Mondays)

| Week | Sourced | Qualified | Parked | Sent | Reply % | Meetings | Inbox health |
|---|---|---|---|---|---|---|---|
| — | 34 | 17 scored | 19 | 0 | — | 0 | no inboxes yet |
