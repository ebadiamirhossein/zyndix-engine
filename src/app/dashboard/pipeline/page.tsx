import type { Metadata } from "next";

export const metadata: Metadata = { title: "Pipeline — Zyndix Engine" };

export default function Page() {
  return (
    <section>
      <h1>Pipeline</h1>
      <p>Nothing here yet.</p>
      <p>This area will hold: Booked and held meetings, proposals, won and lost work, and payments you record.</p>
      <p>U8 adds meetings and the booking stop; U20 adds proposals and opportunities.</p>
    </section>
  );
}
