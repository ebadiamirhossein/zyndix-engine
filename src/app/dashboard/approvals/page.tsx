import type { Metadata } from "next";

export const metadata: Metadata = { title: "Approvals — Zyndix Engine" };

export default function Page() {
  return (
    <section>
      <h1>Approvals</h1>
      <p>Nothing here yet.</p>
      <p>This area will hold: Drafts and proposed actions awaiting review, with the evidence behind each one.</p>
      <p>Approvals run in Telegram today. U17 brings them into the dashboard.</p>
    </section>
  );
}
