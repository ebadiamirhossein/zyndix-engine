import type { Metadata } from "next";

export const metadata: Metadata = { title: "Reports & Settings — Zyndix Engine" };

export default function Page() {
  return (
    <section>
      <h1>Reports & Settings</h1>
      <p>Nothing here yet.</p>
      <p>This area will hold: Cohorts, costs, prompt and policy versions, users and audit logs.</p>
      <p>U21 builds reporting, cost caps and the learning digest.</p>
    </section>
  );
}
