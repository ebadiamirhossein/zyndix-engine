import type { Metadata } from "next";

export const metadata: Metadata = { title: "Campaigns & Journeys — Zyndix Engine" };

export default function Page() {
  return (
    <section>
      <h1>Campaigns & Journeys</h1>
      <p>Nothing here yet.</p>
      <p>This area will hold: Segment, geography, language, offer, sources, budget limits and the touch plan.</p>
      <p>U14 introduces campaigns, enrolments and prospect import.</p>
    </section>
  );
}
