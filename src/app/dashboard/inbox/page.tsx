import type { Metadata } from "next";

export const metadata: Metadata = { title: "Inbox & Tasks — Zyndix Engine" };

export default function Page() {
  return (
    <section>
      <h1>Inbox & Tasks</h1>
      <p>Nothing here yet.</p>
      <p>This area will hold: Replies across channels and the manual actions they generate.</p>
      <p>U7 classifies replies; U20 builds the inbox and task surface.</p>
    </section>
  );
}
