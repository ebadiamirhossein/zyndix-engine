import type { Metadata } from "next";

export const metadata: Metadata = { title: "Companies & Contacts — Zyndix Engine" };

export default function Page() {
  return (
    <section>
      <h1>Companies & Contacts</h1>
      <p>Nothing here yet.</p>
      <p>This area will hold: Evidence, detected tools, open uncertainties, fit and contact history per company.</p>
      <p>U15 introduces the typed evidence model; U16 adds fit and recommendations.</p>
    </section>
  );
}
